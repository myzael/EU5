input_action = {
    name = "tools_new"
    raw_text = "New file"
}

input_action = {
    name = "tools_open"
    raw_text = "Open file"
}

input_action = {
    name = "tools_save"
    raw_text = "Save file"
}

input_action = {
    name = "tools_save_as"
    raw_text = "Save as file"
}

input_action = {
    name = "tools_copy"
    raw_text = "Copy an element"
}

input_action = {
    name = "tools_cut"
    raw_text = "Cut an element"
}

input_action = {
    name = "tools_paste"
    raw_text = "Paste an element"
}

input_action = {
    name = "tools_search"
    raw_text = "Search an element"
}

input_action = {
    name = "tools_close_search"
    raw_text = "Close search window"
}

input_action = {
    name = "tools_search_go_to_next"
    raw_text = "Search next element"
}

input_action = {
    name = "tools_search_go_to_prev"
    raw_text = "Search previous element."
}

input_action = {
    name = "history_tool_undo"
    raw_text = "Undo one frame"
}

input_action = {
    name = "history_tool_redo"
    raw_text = "Redo one frame"
}

input_action = {
    name = "tools_history"
    raw_text = "Show tools history"
}