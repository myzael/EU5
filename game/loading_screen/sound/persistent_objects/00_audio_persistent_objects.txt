# Defines persistent audio objects
#
# music_manager = {                      # name of object in the persistent audio object manager
#     name = "Music Manager"             # name of object in Wwise
#     scope = Asap                       # scope for the object (see below)
#     init_event = start_debug_music     # Wwise event to call when the object is created
#     deinit_event = stop_debug_music    # Wwise event to call when the object is destroyed
# }
#
# scope can be one of:
# Asap    - init as soon as possible on the game start and persist until the game closes
# Menu    - init when the main menu is loaded and persist until the game closes
# InGame  - init when the game world (galaxy) is loaded and persist until the exit to the menu
#
# music_manager is a required object
# If not specified here, it will be created with default settings
#

music_manager = {
	name = "Persistent Music Manager"
	scope = Asap
	init_event = mus_system_start
	# deinit_event = stop_debug_music
}

ambient_manager = {
	name = "Persistent Ambient Manager"
	scope = InGame
	init_event = MasterAmbientSystem_Start_viascopes
	deinit_event = MasterAmbientSystem_Stop
}

#DO NOT EVER REMOVE THIS UI MANAGER OBJECT, THE MESSAGE SYSTEM RELIES ON HAVING IT AS A HARD CODED TARGET
ui_manager = {
	name = "UI Manager"
	scope = Menu
	#init_event = 
	#deinit_event = 
}