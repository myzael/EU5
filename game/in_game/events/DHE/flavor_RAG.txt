namespace = flavor_rag

flavor_rag.1000 = { #Enemy at the Gates
	type = country_event
	title = flavor_rag.1000.title
	desc = flavor_rag.1000.desc
	outcome = neutral

	illustration_tags = {
		10 = interior
		10 = regular
	}

	fire_only_once = yes
	
	historical_info = flavor_rag.1000.historical_info

	dynamic_historical_event = {
		tag = RAG
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 1
	}
	
	immediate = {
		ruler ?= {
			save_scope_as = target_ruler
		}
		c:TUR = {
			ruler ?= {
				save_scope_as = ottoman_ruler
			}
		}
	}

	trigger = {
		is_subject = no
		country_exists = c:TUR
		any_neighbor_country = {
			tag = TUR
		}
		c:TUR = {
			NOT = { is_allied_with = { target = root } }
			is_great_power = yes
			great_power_score > c:RAG.great_power_score
		}
	}
	
	option = {
		name = flavor_rag.1000.a
		historical_option = yes
		add_prestige = prestige_severe_penalty
		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_minor_move_to_right
		}
		c:TUR = {
			trigger_event_non_silently = flavor_rag.1001
		}
	}
	
	option = {
		name = flavor_rag.1000.b
		add_prestige = prestige_mild_bonus
		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_minor_move_to_left
		}
	}
}

flavor_rag.1001 = { #An Offer from Ragusa
	type = country_event
	title = flavor_rag.1001.title
	desc = flavor_rag.1001.desc
	outcome = neutral

	historical_info = flavor_rag.1000.historical_info

	illustration_tags = {
		10 = interior
		10 = regular
	}
	
	major = yes
	major_trigger = { 
		tag = RAG
	}
	
	option = {
		name = flavor_rag.1001.a
		historical_option = yes
		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_minor_move_to_right
		}
		c:RAG = {
			make_subject_of = {
				target = c:TUR
				type = subject_type:tributary
			}
		}
		add_opinion = {
			modifier = opinion_rag_ottoman_partnership
			target = c:RAG
		}
	}
	
	option = {
		name = flavor_rag.1001.b
		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_minor_move_to_left
		}
		add_casus_belli = {
			target = c:RAG
			type = casus_belli:cb_annex
		}
	}
}
