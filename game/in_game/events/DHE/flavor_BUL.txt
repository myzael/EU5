namespace = flavor_bul

flavor_bul.1 = { #Principality of Dobruja
	type = country_event
	title = flavor_bul.1.title
	desc = flavor_bul.1.desc
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.1.historical_info

	dynamic_historical_event = {
		tag = BUL
		from = 1356.1.1
		to = 1400.1.1
		monthly_chance = 10
	}

	trigger = {
		NOT = { country_exists = c:KVN }
		"estate_power(estate_type:nobles_estate)" > 0.50
		stability < 20
	}
	
	illustration_tags = {
		10 = angry
		10 = exterior
	}
	
	immediate = {
		ruler = {
			save_scope_as = target_ruler
		}
		if = {
			limit = {
				NOT = {
					dynasty_exists = terter_dynasty
				}
			}
			location:kaliakra = { create_dynasty_from_location = terter_dynasty }
		}
		create_character = {
			first_name = Balik
			min_age = 40
			birth_location = location:kaliakra
			estate = estate_type:nobles_estate
			dynasty = dynasty:terter_dynasty
			adm = { 20 60 }
			dip = { 20 60 }
			mil = { 50 80 }
			culture = culture:bulgarian
			save_scope_as = target_character
			script = bul_balik
			create_in_limbo = yes
		}
		every_owned_location = {
			limit = {
				OR = {
					province = location:constanta.province
					this = location:pangalia
					this = location:karvuna
					this = location:kaliakra
					this = location:varna
				}
			}
			add_core = c:KVN
		}
	}

	option = {
		name = flavor_bul.1.a
		historical_option = yes
		add_stability = stability_mild_bonus
		add_legitimacy = legitimacy_severe_penalty
		add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_bonus }
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_right }
        create_country_from_cores_in_our_locations = c:KVN
		c:KVN = {
			set_new_ruler = scope:target_character
			make_subject_of = {
				target = root
				type = subject_type:vassal
			}
		}
	}	
	
	option = {
		name = flavor_bul.1.b
		add_stability = stability_severe_penalty
		add_legitimacy = legitimacy_mild_bonus
		add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_penalty }
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_left }
		create_rebel = {
			name = dobrujan_revolt
			category = estate
			estate = nobles_estate
			save_scope_as = target_rebel
		}
		scope:target_character = { change_character_allegiance = scope:target_rebel }
		custom_tooltip = {
			text = flavor_bul.1.b.tt
			every_owned_location = {
				limit = {
					is_core_of = c:KVN
				}
				every_pop = {
					limit = {
						owner = root
					}
					change_pop_allegiance = scope:target_rebel
					add_pop_satisfaction = pop_satisfaction_severe_penalty
				}
			}
		}
	}
	
	option = {
		name = flavor_bul.1.c
		high_risk_option = yes
		add_stability = stability_mild_bonus
		add_legitimacy = legitimacy_severe_penalty
		add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_bonus }
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_right }
        create_country_from_cores_in_our_locations = c:KVN
		c:KVN = {
			set_new_ruler = scope:target_character
			make_subject_of = {
				target = root
				type = subject_type:vassal
			}
		}
		change_player = c:KVN
	}	
}

flavor_bul.2 = { #Tsardom of Vidin
	type = country_event
	title = flavor_bul.2.title
	desc = flavor_bul.2.desc
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.2.historical_info

	dynamic_historical_event = {
		tag = BUL
		from = 1369.1.1
		to = 1400.1.1
		monthly_chance = 10
	}

	trigger = {
		has_ruler = yes
		has_heir = yes
		ruler = {
			age_in_years > 45
		}
		heir = {
			is_child_of = root.ruler
		}
		any_character = {
			is_alive = yes
			is_sibling_of = root.heir
			is_female = no
			is_ruler = no
  			is_heir = no
  			age_in_years >= 15
		}
		NOT = { country_exists = c:VID }
		any_owned_location = {
			is_core_of = c:VID
			count > 6
		}
		owns = location:vidin
	}
	
	illustration_tags = {
		10 = angry
		10 = interior
	}
	
	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
		ruler = {
			save_scope_as = target_ruler
		}
		heir = {
			save_scope_as = target_heir
		}
		random_character = {
			limit = {
				is_alive = yes
				is_sibling_of = root.heir
				is_female = no
				is_ruler = no
  				is_heir = no
  				age_in_years >= 15
			}
			save_scope_as = targer_brother
		}
	}
	
	option = {
		name = flavor_bul.2.a
		historical_option = yes
		add_stability = stability_mild_bonus
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_right }
        create_country_from_cores_in_our_locations = c:VID
		c:VID = {
			set_new_ruler = scope:targer_brother
			make_subject_of = {
				target = root
				type = subject_type:vassal
			}
		}
	}
	
	option = {
		name = flavor_bul.2.b
		add_stability = stability_severe_penalty
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_left }
	}
	
	option = {
		name = flavor_bul.2.c
		high_risk_option = yes
		add_stability = stability_mild_bonus
		change_societal_value = { type = centralization_vs_decentralization value = societal_value_move_to_right }
        create_country_from_cores_in_our_locations = c:VID
		c:VID = {
			set_new_ruler = scope:targer_brother
			make_subject_of = {
				target = root
				type = subject_type:vassal
			}
		}
		change_player = c:VID
	}
}

flavor_bul.3 = { #The Second Golden Age of Bulgaria
	type = country_event
	title = flavor_bul.3.title
	desc = flavor_bul.3.desc
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.3.historical_info

	dynamic_historical_event = {
		tag = BUL
		from = 1345.1.1
		to = 1400.1.1
		monthly_chance = 10
	}

	trigger = {
		has_ruler = yes
		at_war = no
	}
	
	illustration_tags = {
		10 = regular
		10 = interior
	}
	
	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		ruler = {
			save_scope_as = target_ruler
		}
	}
	
	option = {
		name = flavor_bul.3.a
		historical_option = yes
		add_gold = { value = root.monthly_income_trade_and_tax multiply = -8.00 }
		culture = {
			add_cultural_tradition = cultural_tradition_mild_bonus
			add_cultural_influence = cultural_influence_mild_bonus
		}
		scope:target_ruler = {
			add_character_modifier = {
				modifier = bul_patron_of_bulgarias_revival
				years = -1
				mode = add
			}
		}
	}
	
	option = {
		name = flavor_bul.3.b
		add_prestige = prestige_mild_penalty
		change_societal_value = {
			type = traditionalist_vs_innovative
			value = societal_value_move_to_left
		}
	}
}

flavor_bul.4 = { #Gospels of Tsar Ivan Alexander
	type = country_event
	title = flavor_bul.4.title
	desc = flavor_bul.4.desc
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.4.historical_info

	dynamic_historical_event = {
		tag = BUL
		from = 1347.1.1
		to = 1400.1.1
		monthly_chance = 1
	}

	trigger = {
		has_ruler = yes
		ruler = { has_character_modifier = bul_patron_of_bulgarias_revival }
		owns = location:tarnovo
	}
	
	illustration_tags = {
		10 = regular
		10 = interior
	}
	
	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:clergy_estate background = estate_type:clergy_estate }
		ruler = {
			save_scope_as = target_ruler
		}
		character:bul_ivan_alexander ?= {
			save_scope_as = bul_ivan
		}
		create_character = {
			first_name = name_simeon
			artist = writer
			artist_skill = 0.6
			culture = culture:bulgarian
			birth_location = location:tarnovo
			min_age = 50
			save_scope_as = target_artist
			religion = religion:orthodox
			adm = 64
			dip = 56
			mil = 22
			estate = estate_type:clergy_estate
			create_in_limbo = yes
		}
	}
	
	option = {
		name = flavor_bul.4.a
		historical_option = yes
		scope:target_artist = {
			move_country = root
		}
		location:tarnovo = {
			create_art = {
				quality = 80
				type = work_of_art_type:scripture
				key = gospels_of_tsar_ivan_alexander
				artist = scope:target_artist
			}
		}
		culture = {
			add_cultural_tradition = cultural_tradition_weak_bonus
			add_cultural_influence = cultural_influence_weak_bonus
		}
	}
}

flavor_bul.5 = { #Bulgarian-Ottoman Wars
	type = country_event
	title = flavor_bul.5.title
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					has_local_variable = bul_won_against_ottomans
				}
				desc = flavor_bul.5.desc_victory
			}
			triggered_desc = {
				trigger = {
					has_local_variable = bul_lost_against_ottomans
				}
				desc = flavor_bul.5.desc_defeat
			}
		}
	}
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.5.historical_info

	trigger = {
		has_ruler = yes
		country_exists = c:TUR
		c:TUR = {
			has_ruler = yes
			any_owned_location = {
				region = region:balkan_region
			}
		}
	}
	
	illustration_tags = {
		10 = angry
		10 = exterior
	}
	
	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
		ruler = {
			save_scope_as = target_ruler
		}
		c:TUR = {
			ruler = {
				save_scope_as = ottoman_ruler
			}
		}
	}
	
	option = {
		name = flavor_bul.5.a
		historical_option = yes
		change_societal_value = { type = offensive_vs_defensive value = societal_value_move_to_right }
	}
	
	option = {
		name = flavor_bul.5.b
		change_societal_value = { type = offensive_vs_defensive value = societal_value_move_to_left }
	}
}

flavor_bul.6 = { #A Royal Divorce
	type = country_event
	title = flavor_bul.6.title
	desc = flavor_bul.6.desc
	outcome = neutral
	historical_info = flavor_bul.6.historical_info

	fire_only_once = yes

	dynamic_historical_event = {
		tag = BUL
		from = 1337.1.1
		to = 1380.1.1
		monthly_chance = 1
	}
	
	trigger = {
		has_ruler = yes
		character:bul_ivan_alexander ?= {
			is_alive = yes
		}
		character:bul_teodora_of_wallachia ?= { 
			is_alive = yes 
			first_spouse ?= ROOT.ruler
		}
		ruler ?= {
			is_adult = yes
			is_female = no
		}
	}
	
	immediate = {
		character:bul_ivan_alexander ?= {
			save_scope_as = target_ruler
		}
		character:bul_teodora_of_wallachia ?= { 
			save_scope_as = target_oldwife
		}
		create_character = {
			first_name = name_theodora
			adm = 43
			dip = 77
			mil = 12
			min_age = 25
			fertility = 100
			female = yes
			estate = estate_type:peasants_estate
			birth_location = location:tarnovo
			script = bul_sara_theodora
			save_scope_as = target_newwife
			create_in_limbo = yes
		}
	}
	
	option = {
		name = flavor_bul.6.a
		historical_option = yes
		add_stability = stability_severe_penalty
		ruler = {
			divorce_character = scope:target_oldwife 
			scope:target_newwife = { move_country = root }
			marry_character = scope:target_newwife
		}
		add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_penalty }
	}
	
	option = {
		name = flavor_bul.6.b
		ruler = {
			add_dip = -20
		}
		add_legitimacy = legitimacy_severe_penalty
		add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_bonus }
		hidden_effect = {
			kill_character = scope:target_newwife
		}
	}
}

flavor_bul.1000 = { #The Dawn of a New Empire
	type = country_event
	title = flavor_bul.1000.title
	desc = flavor_bul.1000.desc
	outcome = neutral

	fire_only_once = yes

	dynamic_historical_event = {
		tag = BUL
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 10
	}

	trigger = {
		has_ruler = yes
		owns = location:constantinople
		NOT = {
			country_rank = country_rank:rank_empire
		}
		at_war = no
	}
	
	immediate = {
		ruler = {
			save_scope_as = target_ruler
		}
	}
	
	option = {
		name = flavor_bul.1000.a
		set_country_rank = country_rank:rank_empire
		drop_antagonism_bomb = {
			target = capital
			modifier = antagonism_claimed_empire
		}
		add_prestige = prestige_extreme_bonus
		ruler = {
			add_character_modifier = {
				modifier = bul_conqueror_of_constantinople
				years = -1
				mode = add
			}
		}
		if = {
			limit = {
				capital != location:constantinople
			}
			trigger_event_non_silently = flavor_bul.1001
		}
		custom_tooltip = {
			text = flavor_bul.1000.a.tt
			every_neighbor_country = {
				limit = {
					OR = {
						NOT = {
							is_allied_with = { target = root }
							is_subject_of = root
						}
					}
				}
				add_opinion = {
					target = root
					modifier = opinion_bul_proclaimed_an_empire
				}
			}
			every_rival = {
				add_opinion = {
					target = root
					modifier = opinion_bul_proclaimed_an_empire
				}
			}
		}
	}
	
	option = {
		name = flavor_bul.1000.b
		add_prestige = prestige_extreme_bonus
		ruler = {
			add_character_modifier = {
				modifier = bul_conqueror_of_constantinople
				years = -1
				mode = add
			}
		}
		change_societal_value = { type = belligerent_vs_conciliatory value = societal_value_move_to_right }
	}
}

flavor_bul.1001 = { #The City of Emperors
	type = country_event
	title = flavor_bul.1001.title
	desc = flavor_bul.1001.desc
	outcome = neutral

	fire_only_once = yes

	historical_info = flavor_bul.1001.historical_info
	
	option = {
		name = flavor_bul.1001.a
		historical_option = yes
		if = {
			limit = {
				NOT = {
					has_accepted_culture = culture:greek_culture
				}
			}
			add_accepted_culture = culture:greek_culture
		}
		set_capital = location:constantinople
		if = {
			limit = {
				location:constantinople = {
					NOT = { is_core_of = ROOT }
				}
			}
			location:constantinople = { add_core = ROOT }
		}
		location:constantinople = {
			change_development  = development_ultimate_bonus
			change_prosperity = prosperity_severe_bonus
		}
	}
	
	option = {
		name = flavor_bul.1001.b
		capital = {
			change_development  = development_ultimate_bonus
			change_prosperity = prosperity_severe_bonus
		}
	}
}
