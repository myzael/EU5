namespace = flavor_sie

flavor_sie.1 = { # Banca Monte dei Paschi di Siena https://en.wikipedia.org/wiki/Banca_Monte_dei_Paschi_di_Siena
	type = country_event
	title = flavor_sie.1.title
	desc = flavor_sie.1.desc
	outcome = neutral
	historical_info = flavor_sie.1.historical_info
	fire_only_once = yes

	major = yes
	dynamic_historical_event = {
		tag = SIE
		from = 1450.1.1
		to = 1600.1.1
		monthly_chance = 10
	}

	trigger = {
		owns = location:siena
	}

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		location:siena = {
			save_scope_as = monte_paschi_bank_hq
		}

		scope:monte_paschi_bank_hq = {
			create_building_country_in_location = {
				name = {
					name = MONTE_PASCHI
				}
				reforms = {
					banking_country
				}
				save_scope_as = monte_paschi_bank_tag

				hidden_effect = {
					change_country_flag = MONTE_PASCHI
					define_unique_country_tag = MTPSC
					change_heir_selection = heir_selection:bank_selection
					change_government_type = government_type:republic
				}
			}
			scope:monte_paschi_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:monte_paschi_bank_tag
				}
			}
		}
		set_local_variable = {
			name = amount_of_gold_for_monte_paschi
			value = { 250 500 }
		}
	}

	option = {
		name = flavor_sie.1.a

		historical_option = yes

		show_as_tooltip = {
			scope:monte_paschi_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:monte_paschi_bank_tag
				}
			}
		}
		scope:monte_paschi_bank_tag = {
			make_subject_of = {
				target = ROOT
				type = subject_type:state_bank
			}
		}

		unlock_advance_effect = { type = pis_marble_tradition }

		change_gold_effect = { scale = 2 }
	}

	option = {
		name = flavor_sie.1.b
		high_risk_option = yes

		show_as_tooltip = {
			scope:monte_paschi_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:monte_paschi_bank_tag
				}
			}
		}
		scope:monte_paschi_bank_tag = {
			make_subject_of = {
				target = ROOT
				type = subject_type:state_bank
			}
		}
		change_gold_effect = { scale = 2 }

		change_player = scope:monte_paschi_bank_tag
	}

	after = {
		scope:monte_paschi_bank_tag = {
			trigger_event_non_silently = flavor_sie.2
		}
	}
}

flavor_sie.2 = { # Starting Event for the Banca Monte dei Paschi di Siena
	type = country_event
	title = flavor_sie.2.title
	desc = flavor_sie.2.desc
	outcome = neutral

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
	}

	option = {
		name = flavor_sie.2.a

		add_gold = local_var:amount_of_gold_for_monte_paschi

	}

}

#Golden Age of Siena
flavor_sie.3 = {
	type = country_event
	title = flavor_sie.3.title
	desc = flavor_sie.3.desc
	outcome = positive

	dynamic_historical_event = {
		tag = SIE
		from = 1337.1.1
		to = 1450.1.1
		monthly_chance = 75
	}

	fire_only_once = yes

	illustration_tags = {
		10 = happy
		10 = exterior
	}

	trigger = {
		NOT = { has_game_rule = black_death_date_off }	

		owns = location:siena
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }

		location:siena = {
			save_scope_as = target_location
		}
		
		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.3.a
		
		
		custom_tooltip = {
			text = sie_gain_modifier_until_the_black_death
			location:siena = {
				add_location_modifier = {
					mode = add
					modifier = sie_golden_age_of_siena
					years = -1
				}
			}
		}
	}
}

flavor_sie.4 = {
	type = country_event
	title = flavor_sie.4.title
	desc = flavor_sie.4.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1337.1.1
		to = 1500.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		has_had_situation = {
			situation = situation:black_death
		}

		owns = location:siena
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }

		ruler_or_regent ?= {
			save_scope_as = target_character
		}

		location:siena = {
			save_scope_as = target_location
		}
	}

	option = {
		name = flavor_sie.4.a
		
		change_gold_effect = {
			scale = -1
		}

		scope:target_location = {
			change_development = development_mild_bonus
			change_prosperity = prosperity_mild_bonus
		}
	}

	option = {
		name = flavor_sie.4.b
		
		scope:target_location = {
			custom_tooltip = {
				text = sie_all_pops_lose_satisfaction
				every_pop = {
					limit = {
						owner ?= root	
					}
					add_pop_satisfaction = pop_satisfaction_extreme_penalty
				}
			}
		}
	}
}

scripted_effect expand_siena_ports = {
	construct_building = {
		building_type = building_type:sie_sienese_port
		cost_multiplier = 1
	}

	change_development = development_mild_bonus
}

#Port Events
#A series of ports Siena built before and after the Black death
flavor_sie.5 = {
	type = country_event
	title = flavor_sie.5.title
	desc = flavor_sie.5.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1337.1.1
		to = 1600.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		owns = location:grosseto
		
		location:grosseto = {
			NOT = { location_rank = location_rank:rural_settlement }
		}
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		
		location:grosseto = {
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.5.a
		
		scope:target_location = {
			expand_siena_ports = yes
		}

		change_gold_effect = {
			scale = -0.25
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_right
		}

		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_bonus
		}
	}

	option = {
		name = flavor_sie.5.b
		
		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_penalty
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_left
		}
	}
}

flavor_sie.6 = {
	type = country_event
	title = flavor_sie.6.title
	desc = flavor_sie.6.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1337.1.1
		to = 1600.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		owns = location:pitigliano
		
		location:pitigliano = {
			NOT = { location_rank = location_rank:rural_settlement }
		}
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		
		location:pitigliano = {
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.6.a
		
		scope:target_location = {
			expand_siena_ports = yes
		}

		change_gold_effect = {
			scale = -0.25
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_right
		}

		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_bonus
		}
	}

	option = {
		name = flavor_sie.6.b
		
		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_penalty
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_left
		}
	}
}

flavor_sie.7 = {
	type = country_event
	title = flavor_sie.7.title
	desc = flavor_sie.7.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1337.1.1
		to = 1600.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		owns = location:piombino
		
		location:piombino = {
			NOT = { location_rank = location_rank:rural_settlement }
		}
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		
		location:piombino = {
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.7.a
		
		scope:target_location = {
			expand_siena_ports = yes
		}

		change_gold_effect = {
			scale = -0.25
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_right
		}

		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_bonus
		}
	}

	option = {
		name = flavor_sie.7.b
		
		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_penalty
		}

		change_societal_value = {
			type = land_vs_naval
			value = societal_value_move_to_left
		}
	}
}

#Pandolfo Petrucci
flavor_sie.8 = {
	type = country_event
	title = flavor_sie.8.title
	desc = flavor_sie.8.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1470.1.1
		to = 1520.1.1
		monthly_chance = 3
	}

	fire_only_once = yes
	
	illustration_tags = {
		10 = regular
		10 = interior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:burghers_estate }

		location:siena = {
			create_dynasty_from_location = petrucci
		}

		capital ?= {
			save_scope_as = target_capital
		}

		create_character = {
			estate = estate_type:nobles_estate
			first_name = name_pandulf
			dynasty = dynasty:petrucci
			culture = culture:tuscan
			religion = religion:catholic
			birth_date = 1452.2.14
			adm = { 70 80 }
			dip = { 90 100 }
			mil = { 50 60 }
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.8.a
		
		scope:target_character = {
			add_character_modifier = {
				mode = add
				modifier = sie_machiavellian_thinker
				years = -1
			}
		}

		add_estate_satisfaction = {
			type = estate_type:nobles_estate
			value = estate_satisfaction_extreme_penalty
		}

		custom_tooltip = sie_triumphant_return_means_possibility_of_coup
	}

	option = {
		name = flavor_sie.8.b

		trigger = {
			ruler_or_regent ?= {
				has_trait = well_connected
			}	
		}

		add_government_power = government_power_mild_bonus
	}

	option = {
		name = flavor_sie.8.c

		scope:target_character = {
			banish_character = yes
		}

		add_government_power = government_power_mild_bonus

		add_estate_satisfaction = {
			type = estate_type:nobles_estate
			value = estate_satisfaction_mild_bonus
		}
	}
}

#Did you know? The guy whose family was exiled is now seizing power in the country
flavor_sie.9 = {
	type = country_event
	title = flavor_sie.9.title
	desc = flavor_sie.9.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1470.1.1
		to = 1520.1.1
		monthly_chance = 20 #Trigger is the main restriction here
	}

	fire_only_once = yes

	illustration_tags = {
		10 = regular
		10 = interior
	}

	trigger = {
		ruler ?= {
			has_character_modifier = sie_machiavellian_thinker
		}
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }

		ruler ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.9.a
		
		if = {
			limit = {
				government_type = government_type:republic
			}
			change_heir_selection = heir_selection:matrilineal_non_exclusive
		}
		else = {
			add_government_power = government_power_extreme_bonus
			add_stability = stability_extreme_bonus
		}

		add_all_estate_satisfaction = {
			value = estate_satisfaction_mild_penalty
		}
	}
}

#Sienese School of Art
flavor_sie.10 = {
	type = country_event
	title = flavor_sie.10.title
	desc = flavor_sie.10.desc
	outcome = positive

	dynamic_historical_event = {
		tag = SIE
		from = 1370.1.1
		to = 1500.1.1
		monthly_chance = 20 #Trigger is the main restriction here
	}

	fire_only_once = yes

	illustration_tags = {
		10 = happy
		10 = interior
	}

	trigger = {
		has_embraced_institution = institution:renaissance

		owns = location:siena
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:clergy_estate }

		ruler ?= {
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.10.a
		
		location:siena = {
			if = {
				limit = {
					has_building = building_type:art_school
				}
				construct_building = {
					building_type = building_type:arts_academy
					cost_multiplier = 2
					cost_multiplier_reason = "game_concept_event"
				}
			}
			else_if = {
				limit = {
					has_building = building_type:arts_academy
				}
				change_development = development_extreme_bonus
				change_prosperity = prosperity_extreme_bonus
			}
			else = {
				construct_building = {
					building_type = building_type:art_school
					cost_multiplier = 0.25
					cost_multiplier_reason = "game_concept_event"
				}
			}
		}

		add_prestige = prestige_extreme_bonus
	}

	option = {
		name = flavor_sie.10.b

		add_prestige = prestige_mild_penalty
	}
}

#Francesco di Giorgio Martini
flavor_sie.11 = {
	type = country_event
	title = flavor_sie.11.title
	desc = flavor_sie.11.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = SIE
		from = 1470.1.1
		to = 1520.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	illustration_tags = {
		10 = interior
		10 = happy
	}

	trigger = {
		owns = location:siena
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:clergy_estate }

		create_character = {
			estate = estate_type:burghers_estate
			first_name = name_frank
			last_name = di_giorgio_martini
			religion = religion:catholic
			culture = culture:tuscan
			birth_date = 1439.6.7
			birth_location = location:siena
			artist = architect
			artist_skill = { 0.6 0.7 }
			create_in_limbo = yes
			save_scope_as = target_character
		}
	}

	option = {
		name = flavor_sie.11.a
		
		change_gold_effect = {
			scale = -0.1
		}

		scope:target_character = {
			move_country = root
		}

		change_societal_value = {
			type = spiritualist_vs_humanist
			value = societal_value_move_to_left
		}
	}

	option = {
		name = flavor_sie.11.b

		kill_character_silently = {
			target = scope:target_character
		}
		
		change_societal_value = {
			type = spiritualist_vs_humanist
			value = societal_value_move_to_right
		}
	}
}

#Saint Catherine of Siena
flavor_sie.12 = {
	type = country_event
	title = flavor_sie.12.title
	desc = flavor_sie.12.desc
	outcome = positive

	dynamic_historical_event = {
		tag = SIE
		from = 1360.1.1
		to = 1400.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	illustration_tags = {
		10 = exterior
		10 = happy
	}

	trigger = {
		religion = religion:catholic
		
		owns = location:siena
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:peasants_estate background = estate_type:clergy_estate }

		create_character = {
			estate = estate_type:peasants_estate
			first_name = name_catherine
			last_name = di_jacopo
			artist = writer
			artist_skill = { 0.8 0.95 } #Pretty important in medieval history
			culture = culture:tuscan
			religion = religion:catholic
			birth_location = location:siena
			save_scope_as = target_character
			female = yes
		}
	}

	option = {
		name = flavor_sie.12.a
		
		add_religious_influence_if_valid = {
			VALUE = religious_influence_extreme_bonus
		}

		add_estate_satisfaction = {
			type = estate_type:peasants_estate
			value = estate_satisfaction_mild_bonus
		}

		add_estate_satisfaction = {
			type = estate_type:clergy_estate
			value = estate_satisfaction_mild_bonus
		}

		culture = {
			add_cultural_influence = cultural_influence_mild_bonus
			add_cultural_tradition = cultural_tradition_mild_bonus
		}
	}
}