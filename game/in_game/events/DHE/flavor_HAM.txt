namespace = flavor_ham

flavor_ham.1 = { # The Berenberg Bank
	type = country_event
	title = flavor_ham.1.title
	desc = flavor_ham.1.desc
	outcome = neutral
	historical_info = flavor_ham.1.historical_info
	fire_only_once = yes

	major = yes
	dynamic_historical_event = {
		tag = HAM
		from = 1575.1.1
		to = 1625.1.1
		monthly_chance = 10
	}

	trigger = {
		owns = location:hamburg
		has_embraced_institution = institution:banking
	}

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		location:hamburg = {
			save_scope_as = berenberg_bank_hq
		}

		create_character = {
			dynasty = dynasty:berenberg_dynasty
			age = { 20 40 }
			save_scope_as = target_character
		}

		scope:berenberg_bank_hq = {
			create_building_country_in_location = {
				name = {
					name = BERENBERG
				}
				reforms = {
					banking_country
				}
				save_scope_as = berenberg_bank_tag
				set_new_ruler = scope:target_character

				hidden_effect = {
					change_country_flag = BERENBERG
					define_unique_country_tag = BERBE
					change_heir_selection = heir_selection:bank_selection
					change_government_type = government_type:republic
				}
			}
			scope:berenberg_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:berenberg_bank_tag
				}
			}
		}
		set_local_variable = {
			name = amount_of_gold_for_berenberg
			value = { 250 500 }
		}
	}

	option = {
		name = flavor_ham.1.a

		historical_option = yes

		show_as_tooltip = {
			scope:berenberg_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:berenberg_bank_tag
				}
			}
		}
		change_gold_effect = { scale = 2 }
	}

	option = {
		name = flavor_ham.1.b
		high_risk_option = yes

		show_as_tooltip = {
			scope:berenberg_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:berenberg_bank_tag
				}
			}
		}
		change_gold_effect = { scale = 2 }

		change_player = scope:berenberg_bank_tag
	}

	after = {
		scope:berenberg_bank_tag = {
			trigger_event_non_silently = flavor_ham.2
		}
	}
}

flavor_ham.2 = { # Starting Event for The Berenberg Bank
	type = country_event
	title = flavor_ham.2.title
	desc = flavor_ham.2.desc
	outcome = neutral

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
	}

	option = {
		name = flavor_ham.2.a

		add_gold = local_var:amount_of_gold_for_berenberg
	}
}

flavor_ham.3 = { # The Hamburger Bank
	type = country_event
	title = flavor_ham.3.title
	desc = flavor_ham.3.desc
	outcome = neutral
	historical_info = flavor_ham.3.historical_info
	fire_only_once = yes

	major = yes
	dynamic_historical_event = {
		tag = HAM
		tag = HSA
		from = 1610.1.1
		to = 1660.1.1
		monthly_chance = 10
	}

	trigger = {
		owns = location:hamburg
		has_embraced_institution = institution:banking
	}

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
		location:hamburg = {
			save_scope_as = hamburger_bank_hq
		}

		scope:hamburger_bank_hq = {
			create_building_country_in_location = {
				name = {
					name = HAMBURGER_BANK
				}
				reforms = {
					banking_country
				}
				save_scope_as = hamburger_bank_tag

				hidden_effect = {
					change_country_flag = HAMBURGER_BANK
					define_unique_country_tag = HBURG
					change_heir_selection = heir_selection:bank_selection
					change_government_type = government_type:republic
				}
			}
			scope:hamburger_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:hamburger_bank_tag
				}
			}
		}
		set_local_variable = {
			name = amount_of_gold_for_hamburger
			value = { 250 500 }
		}
	}

	option = {
		name = flavor_ham.3.a

		historical_option = yes

		show_as_tooltip = {
			scope:hamburger_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:hamburger_bank_tag
				}
			}
		}
		scope:hamburger_bank_tag = {
			make_subject_of = {
				target = ROOT
				type = subject_type:state_bank
			}
		}
		change_gold_effect = { scale = 2 }
	}

	option = {
		name = flavor_ham.3.b
		high_risk_option = yes

		show_as_tooltip = {
			scope:hamburger_bank_hq = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner = scope:hamburger_bank_tag
				}
			}
		}
		scope:hamburger_bank_tag = {
			make_subject_of = {
				target = ROOT
				type = subject_type:state_bank
			}
		}
		change_gold_effect = { scale = 2 }

		change_player = scope:hamburger_bank_tag
	}

	after = {
		scope:hamburger_bank_tag = {
			trigger_event_non_silently = flavor_ham.4
		}
	}
}

flavor_ham.4 = { # Starting Event for The Hamburger Bank
	type = country_event
	title = flavor_ham.4.title
	desc = flavor_ham.4.desc
	outcome = neutral

	illustration_tags = {
        10 = regular
        10 = interior
    }

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
	}

	option = {
		name = flavor_ham.4.a

		add_gold = local_var:amount_of_gold_for_hamburger
	}
}
