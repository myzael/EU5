namespace = flavor_ach

flavor_ach.1 = {
    type = country_event
    title = flavor_ach.1.title
    desc = flavor_ach.1.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH 
        from = 1337.6.1
        to = 1339.1.1
        monthly_chance = 20
    }

    illustration_tags = {
        10 = interior
    }

    trigger = {
        character:epi_nikephoros_ii_orsini = {
            is_alive = yes
            is_ruler = no
        }
        country_exists = c:BYZ
        country_exists = c:EPI
        c:EPI = { is_subject_of = c:BYZ }
        ruler ?= character:ach_catherine_valois
    }

    immediate = {
        event_illustration_estate_background_effect = {
            background = estate_type:nobles_estate
        }
        character:epi_nikephoros_ii_orsini = {
            save_scope_as = target_character
        }
        ruler = {
            save_scope_as = target_character2
        }
        c:EPI = { 
            save_scope_as = target_country
        }
        c:BYZ = {
            save_scope_as = target_country2
        }
        save_scope_as = target_country3
    }

    option = {
        name = flavor_ach.1.a
        set_variable = {
            name = orsini_in_achaea
            value = 1
            years = 25
        }
        drop_antagonism_bomb = {
            modifier = antagonism_opened_war_path
            target = capital
        }
        scope:target_country3 = {
            add_casus_belli = {
                target = scope:target_country
                type = casus_belli:cb_insulted_us
            }
        }

        scope:target_country = {
            trigger_event_non_silently = {
                id = flavor_ach.2
            }
            overlord ?= {
                trigger_event_non_silently = {
                    id = flavor_ach.2
                }
            }
        }
    }

    option = {
        name = flavor_ach.1.b
        
        add_prestige = prestige_weak_penalty
    }
}

flavor_ach.2 = {
    type = country_event
    title = flavor_ach.2.title
    desc = flavor_ach.2.desc
    outcome = neutral

    illustration_tags = {
        10 = exterior
    }

    option = {
        name = flavor_ach.2.a
        
        show_as_tooltip = {
            scope:target_country3 = {
                add_casus_belli = {
                    target = scope:target_country
                    type = casus_belli:cb_insulted_us
                }
            }
        }
    }
}

scripted_trigger is_latin_state_check = {
    culture != culture:greek_culture
    religion = religion:catholic
    capital ?= {
        is_in_scripted_geography = scripted_geography:greece_geography
    }
}

flavor_ach.3 = {
    type = country_event
    title = flavor_ach.3.title
    desc = flavor_ach.3.desc
    outcome = neutral

    trigger = {
        is_latin_state_check = yes
        legitimacy <= 35
        estate_satisfaction:peasants_estate <= 0.25
        ruler ?= { has_dynasty = yes }
        is_subject = no
        NOT = { has_variable = had_greek_insurrection }
    }

    illustration_tags = {
        10 = exterior
        10 = angry
    }

    immediate = {
        event_illustration_estate_effect = {
            foreground = estate_type:peasants_estate
            background = estate_type:peasants_estate
        }
        set_variable = had_greek_insurrection
        ruler = {
            save_scope_as = target_character
        }
        create_character = {
            estate = estate_type:nobles_estate
            culture = culture:greek_culture
            religion = religion:orthodox
            adm = { 20 30 }
            dip = { 40 70 }
            mil = { 60 80 }
            save_scope_as = target_character2
        }
    }

    option = {
        name = flavor_ach.3.a
        
        ai_will_select = {
            value = 40
        }

        set_new_ruler = scope:target_character2
        change_culture = culture:greek_culture
        change_religion = religion:orthodox
        if = {
            limit = {
                has_estate_privilege = estate_privilege:lat_compromise_with_greek_populace
            }
            revoke_estate_privilege = estate_privilege:lat_compromise_with_greek_populace
         }
        if = {
            limit = {
                has_estate_privilege = estate_privilege:lat_latin_crusader_elite
            }
            revoke_estate_privilege = estate_privilege:lat_latin_crusader_elite
         }
    }

    option = {
        name = flavor_ach.3.b
        
        ai_will_select = {
            value = 60
        }

        change_gold_effect = {
            scale = -6
        }

        add_legitimacy = legitimacy_mild_bonus
    }
}

flavor_ach.4 = {
    type = country_event
    title = flavor_ach.4.title
    desc = flavor_ach.4.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1337.1.1
        to = 1338.1.1
        monthly_chance = 75
    }

    illustration_tags = {
        10 = interior
    }

    trigger = {
        ruler ?= character:ach_catherine_valois
    }

    immediate = {
        event_illustration_estate_background_effect = {
            background = estate_type:nobles_estate
        }
        ruler = {
            save_scope_as = target_character
        }
        scripted_geography:greece_geography = {
            every_country_with_capital_in_geography = {
                limit = {
                    religion = religion:catholic
                }
                add_to_list = latin_countries_in_greece
            }
        }
    }

    option = {
        name = flavor_ach.4.a
        
        add_prestige = prestige_severe_bonus

        every_in_list = {
            list = latin_countries_in_greece
            add_opinion_mutual_effect = {
                target = ROOT
                modifier = BYZ_latin_emperor_opinion
            }
        }
    }
}

flavor_ach.5 = { # The Titular Empire
    type = country_event
    title = flavor_ach.5.title
    desc = flavor_ach.5.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1337.1.1 
        to = 1351.1.1 
        monthly_chance = 100
    }


    trigger = {
        at_war = no
        has_ruler = yes
        has_heir = yes
        country_exists = c:NAP
        country_exists = c:ALB
        country_exists = c:ATH
        country_exists = c:BYZ
        character:ach_catherine_valois = {
            is_alive = yes
            is_ruler = yes
        }   
        character:ach_robert_taranto = {
            is_alive = yes
        }    
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        heir ?= {
            save_scope_as = ach_heir
        }
        character:alb_filippo_i_taranto = {
            save_scope_as = scope_philip_latin_emperor
        }
        character:alb_john_gravina = {
            save_scope_as = scope_alb_king
        }
        c:NAP ?= {
            save_scope_as = naples_country
        }
        c:ALB ?= {
            save_scope_as = albania_country
        }
        c:ATH ?= {
            save_scope_as = ath_country
        }
        c:BYZ ?= {
            save_scope_as = byz_country
        }
    }
    

    option = { # Focus on ruling Achaea
        name = flavor_ach.5.a    
        historical_option = yes        
        ai_chance = {
            base = 70
        }         
        add_country_modifier = { modifier = ach_sale_of_albania years = 20 mode = add_and_extend }   
        add_country_modifier = { modifier = ach_focus_on_rule years = 12 mode = add_and_extend }
    }

    option = { # Strive to rebuild relations
        name = flavor_ach.5.b   
        ai_chance = {
            base = 10
        }
        add_country_modifier = { modifier = ach_sale_of_albania years = 20 mode = add_and_extend }               
        add_country_modifier = { modifier = ach_focus_on_diplomacy years = 12 mode = add_and_extend }
    }

    option = { # Pave the way for reconquest
        name = flavor_ach.5.c         
        ai_chance = {
            base = 20
        }
        add_country_modifier = { modifier = ach_sale_of_albania years = 20 mode = add_and_extend }               
        add_country_modifier = { modifier = ach_focus_on_restoration years = 12 mode = add_and_extend }
    }    
}

flavor_ach.6 = { # Ties to the Naples Court
    type = country_event
    title = flavor_ach.6.title
    desc = flavor_ach.6.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1338.1.1 
        to = 1351.1.1 
        monthly_chance = 12
    }


    trigger = {
        has_ruler = yes
        country_exists = c:NAP
        c:NAP ?= {
            has_ruler = yes
        }        
        is_subject = yes
        overlord = c:NAP
        character:ach_catherine_valois = {
            is_alive = yes
            is_ruler = yes
        } 
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:NAP ?= {
            save_scope_as = naples_country
            ruler ?= {
                save_scope_as = nap_ruler
            }
        }
    }
    

    option = { # Allow Catherine to curry favor with the Naples court
        name = flavor_ach.6.a    
        historical_option = yes        
        ai_chance = {
            base = 70
        }         
        add_opinion = { target = c:NAP modifier = ach_nap_court_ties }
        add_trust_equilibrium = {
            modifier = trust_ach_naples_court_ties
            target = c:NAP
        }
        c:NAP = {
            add_opinion = { target = c:ACH modifier = ach_nap_court_ties }
            add_trust_equilibrium = {
                modifier = trust_ach_naples_court_ties
                target = c:ACH
            }
        }        
    }

    option = { # Use Catherine influence to gain monetary support
        name = flavor_ach.6.b   
        ai_chance = {
            base = 10
        }
        add_opinion = { target = c:NAP modifier = ach_nap_court_ties_monetary }
        c:NAP = {
            add_opinion = { target = c:ACH modifier = ach_nap_court_ties_monetary }
        } 
        add_gold = {
            value = c:NAP.monthly_income_trade_and_tax
            multiply = 6
            min = 100
            max = 1000
        }   
    }

    option = { # Catherine reputation would be better used to bolster the court
        name = flavor_ach.6.c         
        ai_chance = {
            base = 20
        }
        add_prestige = prestige_mild_bonus
        add_legitimacy = legitimacy_severe_bonus
    }    
}

flavor_ach.7 = { # Secession in Epirus
    type = country_event
    title = flavor_ach.7.title
    desc = flavor_ach.7.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1340.1.1 
        to = 1370.1.1 
        monthly_chance = 18
    }


    trigger = {
        has_ruler = yes
        at_war = no
        country_exists = c:EPI
        OR = {
            NOT = { country_exists = c:CEP }
            c:CEP ?= {
                is_subject_of = root
            }
        }
        character:epi_nikephoros_ii_orsini ?= {
            is_alive = yes
            OR = {
                owner = c:ACH
                owner = c:CEP
            }
        }
        has_variable = orsini_in_achaea
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:BYZ ?= {
            save_scope_as = byz_country
        }
        character:epi_nikephoros_ii_orsini = {
            save_scope_as = nikephoros_ii_orsini
        }
        c:EPI ?= {
            save_scope_as = epirus_country
            area:northern_greece_area = {
                every_province_in_area = {
                    limit = {
                        owner = c:EPI
                    }
                    add_to_list = revolt_provinces
                }
            }
            create_rebel = {
                category = pretender
                name = nikephoros_ii_orsini_revolt
                save_scope_as = nikephoros_ii_orsini_rebels
            } 
        }   
    }
    

    option = { # Sponsor support for Orsini in Epirus
        name = flavor_ach.7.a    
        historical_option = yes        
        ai_chance = {
            base = 70
        }
        add_gold = -100
        add_prestige = prestige_severe_bonus
        hidden_effect = {
            scope:nikephoros_ii_orsini = {
                move_country = c:EPI
                change_character_allegiance = scope:nikephoros_ii_orsini_rebels
            }     
        }
        c:EPI ?= {
            create_instant_rebellion = {
                list = revolt_provinces
                rebel = scope:nikephoros_ii_orsini_rebels
            }
        }
        set_variable = {
            name = supported_orsini_revolt
            value = 1
            years = 15
        }
        scope:byz_country = { trigger_event_non_silently = { id = flavor_ach.9 days = 3 } }         
    }

    option = { # Offer to press Orsini claim to Epirus
        name = flavor_ach.7.b   
        ai_chance = {
            base = 10
        }
        add_stability = stability_mild_penalty
        add_casus_belli = {
            target = c:EPI
            type = casus_belli:cb_subjugation
        }
    }

    option = { # We cannot finance his claims
        name = flavor_ach.7.c         
        ai_chance = {
            base = 20
        }
        add_prestige = prestige_mild_penalty
        add_stability = stability_severe_bonus
    }    
}

flavor_ach.8 = { # #Orsini seizes Epirus ## If Orsini revolt succeeds
    type = country_event
    title = flavor_ach.8.title
    desc = flavor_ach.8.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1340.1.1 
        to = 1370.1.1 
        monthly_chance = 100
    }


    trigger = {
        has_ruler = yes
        country_exists = c:EPI
        country_exists = c:BYZ
        has_variable = supported_orsini_revolt
        character:epi_nikephoros_ii_orsini ?= {
            is_alive = yes
        }        
        c:EPI ?= {
            ruler ?= character:epi_nikephoros_ii_orsini
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:EPI = {
            save_scope_as = epirus_country
            overlord = { cancel_subject = c:EPI }
        }
        c:BYZ ?= {
            save_scope_as = byz_country
            add_casus_belli =  { 
                target = c:EPI
                type = casus_belli:cb_subject_broke_free	
            } 
        }
        character:epi_nikephoros_ii_orsini = {
            save_scope_as = nikephoros_ii_orsini
        } 
    }
    

    option = { # Let us establish a formal alliance with Epirus
        name = flavor_ach.8.a  
        trigger = {
            is_free_or_tributary_trigger = yes
        }        
        ai_chance = {
            base = 50
        }     
        if = {
            limit = {
                c:EPI = { is_rival_of = root }
            }
            remove_rival = c:EPI
        }
        if = {
            limit = {
                is_rival_of = c:EPI
            }
            c:EPI = { remove_rival = root }
        }
        create_alliance = { target = c:EPI }
        add_prestige = prestige_extreme_bonus
        add_opinion = { target = c:EPI modifier = ach_epirus_support_rebel_victory }
        c:EPI = {
            add_opinion = { target = c:ACH modifier = ach_epirus_support_rebel_victory }
        } 
    }

    option = { # We shall only maintain close relations with Orisini
        name = flavor_ach.8.b
        trigger = {
            is_free_or_tributary_trigger = no
        }  
        ai_chance = {
            base = 50
        }
          if = {
            limit = {
                c:EPI = { is_rival_of = root }
            }
            remove_rival = c:EPI
        }
        if = {
            limit = {
                is_rival_of = c:EPI
            }
            c:EPI = { remove_rival = root }
        }      
        add_prestige = prestige_mild_bonus
        add_opinion = { target = c:EPI modifier = ach_epirus_support_rebel_victory }
        c:EPI = {
            add_opinion = { target = c:ACH modifier = ach_epirus_support_rebel_victory }
        }                      
    }
    option = { # Now is our chance to attack during the vaccum!
        name = flavor_ach.8.c
        ai_chance = {
            base = 5
        } 
        add_stability = stability_extreme_penalty
        add_casus_belli = {
            target = c:EPI
            type = casus_belli:cb_lat_reconquest
        } 
          c:EPI = {
            add_opinion = { target = c:ACH modifier = ach_epirus_support_rebel_victory_betrayed }
        }                              
    }
}

flavor_ach.9 = { # For Byzantium # Revolt in Epirus
    type = country_event
    title = flavor_ach.9.title
    desc = flavor_ach.9.desc
    outcome = neutral

    fire_only_once = yes

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        } 
        c:ACH ?= {
            ruler ?= {
                save_scope_as = ach_scope_ruler
            }
        }  
        c:EPI ?= {
            save_scope_as = epirus_country
            ruler ?= {
                save_scope_as = epi_scope_ruler
            }
        }  
        character:epi_nikephoros_ii_orsini = {
            save_scope_as = nikephoros_ii_orsini
        }      
    }
    

    option = { # Assist the <epirus> governor!
        name = flavor_ach.9.a 
        join_war_with = {
            target = scope:epirus_country
            war = scope:epirus_country.civil_war
            call_in_subjects = yes
            notify_opposing_side = yes
        }
        custom_tooltip = flavor_ach_9_tt      
    }

    option = { # Not of our concern
        name = flavor_ach.9.b  
        add_prestige = prestige_mild_penalty       
    }    
}

flavor_ach.10 = { # Reconquest of the Peloponnese # Territory lost to Byzantium for Morea
    type = country_event
    title = flavor_ach.10.title
    desc = flavor_ach.10.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1343.1.1 
        to = 1370.1.1 
        monthly_chance = 18
    }


    trigger = {
        at_war = no
        country_exists = c:BYZ
        NOT = { has_truce_with = c:BYZ }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:BYZ ?= {
            save_scope_as = byz_country
        }
    }
    

    option = { # We must strike them while their weak!
        name = flavor_ach.10.a    
        historical_option = yes  
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = 0.5
        }   
        area:morea_area = {
            every_location_in_area = {
                limit = {
                    top_owner ?= c:BYZ
                }
                add_core = root
            }
        }  
        add_casus_belli = {
            target = c:BYZ
            type = casus_belli:cb_lat_reconquest
        } 
        add_opinion = { target = c:BYZ modifier = ach_claimed_the_peloponnese }            
    }
    option = { # It would be unwise to anger the Romans
        name = flavor_ach.10.b
        add_stability = stability_severe_bonus
        add_prestige = prestige_weak_penalty   
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = -1
        }          
    }
}

flavor_ach.11 = { # Exploit the Byzantine Civil war # Opportunity should Byzantium enter a civil war
    type = country_event
    title = flavor_ach.11.title
    desc = flavor_ach.11.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1343.1.1 
        to = 1380.1.1 
        monthly_chance = 12
    }


    trigger = {
        at_war = no
        c:BYZ ?= {
            stability < 30
            has_ruler = yes
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:BYZ ?= {
            save_scope_as = byz_country
            ruler ?= {
                save_scope_as = byz_ruler
            }
        }
    }
    

    option = { # Now is the time to crush the dying Phoenix!
        name = flavor_ach.11.a    
        historical_option = yes   
        add_casus_belli = {
            target = c:BYZ
            type = casus_belli:cb_lat_reconquest
        } 
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_ach_byzantine_civil_war_intervention
        }	
        add_country_modifier = { modifier = ach_crush_the_purple_phoenix years = 10 mode = add_and_extend }           
    }
    option = { # Lets avoid involving ourselves in byzantine internal affairs.
        name = flavor_ach.11.b
        add_stability = stability_mild_bonus
        add_prestige = prestige_weak_penalty
        add_country_modifier = { modifier = ach_avoided_byzantine_civil_war years = 10 mode = add_and_extend }               
    }
}

flavor_ach.12 = { # The Greek Populace of Achaea ## Appeasing the Greeks
    type = country_event
    title = flavor_ach.12.title
    desc = flavor_ach.12.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1345.1.1 
        to = 1375.1.1 
        monthly_chance = 6
    }


    trigger = {
        at_war = no
        stability > 20
        NOT = { culture = { has_culture_group = culture_group:greek_group } }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:peasants_estate background = estate_type:peasants_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
    }
    

    option = { # Maintain the current status quo.
        name = flavor_ach.12.a    
        historical_option = yes
        add_stability = stability_mild_bonus 
        add_legitimacy = legitimacy_mild_bonus  
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_penalty }
        add_estate_satisfaction = { type = estate_type:clergy_estate value = estate_satisfaction_weak_penalty }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_weak_penalty }           
    }

    option = { # Favor the French elite
        name = flavor_ach.12.b     
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_bonus }
        add_estate_satisfaction = { type = estate_type:clergy_estate value = estate_satisfaction_severe_bonus }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_mild_penalty }
        add_country_modifier = { modifier = ach_favored_the_french years = 10 mode = add_and_extend }           
    }
    option = { # Strive for a compromise with the greek elite
        name = flavor_ach.12.c
        add_prestige = prestige_mild_penalty
        add_legitimacy = legitimacy_mild_penalty
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_penalty }
        add_estate_satisfaction = { type = estate_type:clergy_estate value = estate_satisfaction_mild_penalty }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_severe_bonus }
        add_country_modifier = { modifier = ach_compromise_wth_the_greeks years = 15 mode = add_and_extend }  
        unlock_estate_privilege_effect = { type = lat_compromise_with_greek_populace }
        grant_estate_privilege = estate_privilege:lat_compromise_with_greek_populace
        culture:greek_culture = {
            change_cultural_view = {
                target = root.culture
                change = 1
            }
        }             
    }
}

flavor_ach.13 = { # Catalan Rule over Athens
    type = country_event
    title = flavor_ach.13.title
    desc = flavor_ach.13.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1345.1.1 
        to = 1537.1.1 
        monthly_chance = 25
    }


    trigger = {
        at_war = no
        country_exists = c:ATH
        c:ATH ?= {
            has_reform = government_reform:ath_rule_of_the_catalans
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:ATH = {
            save_scope_as = athens_country
        }
    }
    

    option = { # The Catalans are more friends than foe.
        name = flavor_ach.13.a    
        historical_option = yes
        add_opinion = { target = c:ATH modifier = ach_ath_frankokratia_ties }
        c:ATH = {
            add_opinion = { target = c:ACH modifier = ach_ath_frankokratia_ties }
        }
    }

    option = { # We should restore French rule to Athens.
        name = flavor_ach.13.b     
        custom_tooltip = ach_vassalize_athens_for_reward
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_bonus }  
        add_casus_belli = {
            target = c:ATH
            type = casus_belli:cb_subjugation
        }  
        set_variable = {
            name = ach_vassalize_athens_for_reward
            value = 1
            years = 50
        }                 
    }
    option = { # Lets expel the Catalans company from Athens and seize the lands.
        name = flavor_ach.13.c
        add_prestige = prestige_weak_bonus
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_bonus }
        add_casus_belli = {
            target = c:ATH
            type = casus_belli:cb_lat_reconquest
        }             
    }
}

flavor_ach.14 = { # French rule restored over Athens. ## Dynamic reward event for vassalizing Athens
    hide_portraits = yes
    type = country_event
    title = flavor_ach.14.title
    desc = flavor_ach.14.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1345.1.1 
        to = 1537.1.1 
        monthly_chance = 50
    }


    trigger = {
        at_war = no
        country_exists = c:ATH
        has_variable = ach_vassalize_athens_for_reward
        c:ATH ?= {
            is_subject_of = root
            is_human = no
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:ATH ={
            save_scope_as = athens_country
        } 
        if = {
            limit = {
                any_character = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:french
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
            }
            random_character = {
                limit = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:french
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
                save_scope_as = french_athens_new_ruler
            }
        }
        else = {
            create_character = {
                adm = { 45 80 }
                dip = { 45 67 }
                mil = { 45 85 }
                age = { 30 40 }
                culture = culture:french
                religion = religion:catholic
                estate = estate_type:nobles_estate
                save_scope_as = french_athens_new_ruler
                create_in_limbo = yes
            }
        }   
        if = {
            limit = {
                any_character = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:greek_culture
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
            }
            random_character = {
                limit = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:greek_culture
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
                save_scope_as = greeks_athens_new_ruler
            }
        }
        else = {
            create_character = {
                adm = { 45 80 }
                dip = { 45 67 }
                mil = { 45 85 }
                age = { 30 40 }
                culture = culture:greek_culture
                religion = religion:catholic
                estate = estate_type:nobles_estate
                save_scope_as = greeks_athens_new_ruler
                create_in_limbo = yes
            }
        }     
    }
    

    option = { # Appoint a French noble to serve as the new duke.
        name = flavor_ach.14.a      
        c:ATH ?= {
            set_new_ruler = scope:french_athens_new_ruler
            change_culture = culture:french
            if = {
                limit = { has_reform = government_reform:ath_rule_of_the_catalans }
                remove_reform = government_reform:ath_rule_of_the_catalans
            }
        }  
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_bonus } 
    }

    option = { # Appoint a French noble and impose our cultural policy over Athens.
        name = flavor_ach.14.b
        trigger = {
            is_human = yes
            has_estate_privilege = estate_privilege:lat_compromise_with_greek_populace 
        }    
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_bonus } 
        c:ATH ?= {
            set_new_ruler = scope:french_athens_new_ruler
            change_culture = culture:french
            unlock_estate_privilege_effect = { type = lat_compromise_with_greek_populace }
            grant_estate_privilege = estate_privilege:lat_compromise_with_greek_populace
            if = {
                limit = { has_reform = government_reform:ath_rule_of_the_catalans }
                remove_reform = government_reform:ath_rule_of_the_catalans
            } 
        }           
    }
    option = { # Grant the lands to a Greek noble instead.
        name = flavor_ach.14.c
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_penalty } 
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_severe_bonus }
        c:ATH ?= {
            set_new_ruler = scope:greeks_athens_new_ruler
            change_culture = culture:greek_culture
            if = {
                limit = { has_reform = government_reform:ath_rule_of_the_catalans }
                remove_reform = government_reform:ath_rule_of_the_catalans
            } 
        }                     
    }
}

flavor_ach.15 = { # The Passing of the Latin Emperorship
    type = country_event
    title = flavor_ach.15.title
    desc = flavor_ach.15.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        from = 1337.1.1 
        to = 1437.1.1 
        monthly_chance = 100
    }


    trigger = {
        at_war = no
        character:ach_robert_taranto = {
            is_alive = yes
        } 
        ruler = character:ach_robert_taranto
        has_ruler = yes    
        character:ach_catherine_valois = {
            is_alive = no
            is_ruler = no
        } 
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        character:ach_catherine_valois = {
            save_scope_as = latin_empress_catherine_valois
        }
        character:alb_filippo_i_taranto = {
            save_scope_as = scope_philip_latin_emperor
        }
    }
    

    option = { # Host a grand ceremony for Robert!
        name = flavor_ach.15.a 
        add_prestige = prestige_extreme_bonus
        add_stability = stability_mild_bonus
        add_country_modifier = { modifier = ach_crowning_of_a_new_emperor years = 15 mode = add }  
    }

    option = { # Use the ceremony to bolster efforts to restore the empire.
        name = flavor_ach.15.b
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = 1
        }
        change_societal_value = {
            type = belligerent_vs_conciliatory
            value = societal_value_move_to_left
        }
        add_country_modifier = { modifier = ach_bolster_calls_for_restoration years = 15 mode = add }  
    }    
    option = { # Leverage the title for political gain
        name = flavor_ach.15.c
        add_legitimacy = legitimacy_extreme_bonus
        add_country_modifier = { modifier = ach_leverage_latin_title_political years = 15 mode = add }       
    }
}

flavor_ach.16 = { # Pope Takes Interest in restoration of the Latin empire
    type = country_event
    title = flavor_ach.16.title
    desc = flavor_ach.16.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        tag = THE
        from = 1337.1.1 
        to = 1351.1.1 
        monthly_chance = 25
    }


    trigger = {
        has_reform = government_reform:ach_heirs_of_the_latin_empire
        at_war = no
        has_ruler = yes
        scripted_geography:frankokratia_geography = { # Retool to be for Frankokratia region
            any_location_in_scripted_geography = {
                owner ?= ROOT
                count > 32
            }
        }  
        country_exists = c:PAP
        country_exists = c:BYZ
        c:PAP ?= {
            has_ruler = yes
        }   
        NOT = { country_exists = c:LAT }
        religion = religion:catholic
        NOT = { num_locations >= 70 }   
        NOT = { owns = location:constantinople }   
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:clergy_estate background = estate_type:clergy_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:PAP ?= {
          save_scope_as = scope_papal_states
          ruler ?= {
                save_scope_as = the_pope
            }  
        }
        c:BYZ ?= {
          save_scope_as = scope_byz
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }        
    }
    

    option = { # Accept the Pope offer to restore the Latin empire
        name = flavor_ach.16.a 
        custom_tooltip = lat_aggressive_conversion_policies_tt
        unlock_government_reform_effect = { type = lat_papal_restoration_claimant }
        add_reform = government_reform:lat_papal_restoration_claimant
        add_country_modifier = { modifier = lat_support_from_the_papacy years = 25 mode = add } 
        add_gold = {
            value = c:PAP.monthly_income_trade_and_tax
            multiply = 6
            min = 500
            max = 1200
        } 
        set_variable = {
            name = lat_accepted_pope_latin_empire_proposal
            value = 1
            years = 100
        }  
        add_opinion = { target = c:PAP modifier = ach_papal_latin_restoration_guarantee }
        c:PAP = {
            add_opinion = { target = c:ACH modifier = ach_papal_latin_restoration_guarantee }
        } 
    }
  
    option = { # Reject his offer, we shall go our own path
        name = flavor_ach.16.b
        add_prestige = prestige_severe_bonus
        add_legitimacy = legitimacy_mild_penalty
        c:PAP = {
            add_opinion = { target = c:ACH modifier = ach_rejected_papacy_offer }
        }        
    }
}

flavor_ach.17 = { # Constantinople reclaimed!
    type = country_event
    title = flavor_ach.17.title
    desc = flavor_ach.17.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ACH
        tag = THE
        from = 1337.1.1 
        to = 1537.1.1 
        monthly_chance = 100
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        NOT = { country_exists = c:LAT } 
        has_reform = government_reform:ach_heirs_of_the_latin_empire
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }    
    }
    

    option = { # It is time we reestablish our rule over Constantinople.
        name = flavor_ach.17.a 
        add_prestige = prestige_radical_bonus
        form_country = formable_country:LAT_f
        if = {
            limit = { 
                NOT = { 
                    capital = location:constantinople
                } 
            }
            set_capital = location:constantinople
        }
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        }
        if = {
            limit = { country_rank_level < 4 }
            set_country_rank_effect = { rank = country_rank:rank_empire }
        }
        if = {
            limit = { has_reform = government_reform:lat_papal_restoration_claimant }
            remove_reform = government_reform:lat_papal_restoration_claimant
        }
        if = {
            limit = { has_reform = government_reform:partitio_reform }
            remove_reform = government_reform:partitio_reform
        }
        unlock_government_reform_effect = { type = lat_restored_rule_of_the_latins }
        add_reform = government_reform:lat_restored_rule_of_the_latins
        if = {
            limit = { 
                has_reform = government_reform:ach_heirs_of_the_latin_empire
            }
            hidden_effect = {
                change_country_color = map_LAT_titular_restoration
            }            
        }
        if = {
            limit = { has_reform = government_reform:ach_heirs_of_the_latin_empire }
            remove_reform = government_reform:ach_heirs_of_the_latin_empire
        }        
    }
  
    option = { # We should strengthen our position before formally restoring the empire.
        name = flavor_ach.17.b
        add_prestige = prestige_extreme_bonus
        add_stability = stability_extreme_bonus
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        }  
        set_variable = {
            name = lat_reduced_requirements_to_form
            value = 1
            years = 300
        }     
        custom_tooltip = latin_empire_reformation_conditions_unchanged_tt      
    }

    option = { # Sack it once more
        name = flavor_ach.17.c
        c:PAP = {
            add_opinion = { target = c:ACH modifier = ach_pap_sacked_constantinople_again }
        } 
        add_gold = {
            value = 1500
        }
        location:constantinople = {
            change_development = development_ultimate_penalty
            change_prosperity = prosperity_radical_penalty
        }
        add_prestige = prestige_extreme_bonus
        add_stability = stability_mild_penalty
        add_legitimacy = legitimacy_severe_penalty
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        } 
        set_variable = {
            name = lat_reduced_requirements_to_form
            value = 1
            years = 300
        } 
        custom_tooltip = latin_empire_reformation_conditions_unchanged_tt          
    }    
}
