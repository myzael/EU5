namespace = flavor_luc

flavor_luc.1 = { #Silk Manufactoring of Lucca
	type = country_event
	title = flavor_luc.1.title
	desc = flavor_luc.1.desc
	outcome = neutral

	fire_only_once = yes

	dynamic_historical_event = {
		tag = LUC
		from = 1350.1.1
		to = 1500.1.1
		monthly_chance = 3
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

    trigger = {
		owns = location:lucca
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }

		ruler_or_regent ?= {
            save_scope_as = target_character
        }
	}

	option = {
		name = flavor_luc.1.a

		location:lucca = {
            construct_building = { #Placeholder building. Should be removed once CAESAR-65011 Jira is implemented
                building_type = building_type:lucca_silk_production_guild
                cost_multiplier = 0.5
                cost_multiplier_reason = "burghers_cheap_sponsorship"
            }
        }

        add_estate_satisfaction = {
            type = estate_type:burghers_estate
            value = estate_satisfaction_mild_bonus
        }
	}

	option = {
		name = flavor_luc.1.b

		add_estate_satisfaction = {
            type = estate_type:burghers_estate
            value = estate_satisfaction_mild_penalty
        }
	}
}

#Costly Fabric in Lucca. Decaying modifier for Lucca
flavor_luc.2 = {
	type = country_event
	title = flavor_luc.2.title
	desc = flavor_luc.2.desc
	outcome = positive

	dynamic_historical_event = {
		tag = LUC
		from = 1337.1.1
		to = 1450.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		owns = location:lucca

		location:lucca = {
			has_building_with_at_least_one_level = fine_cloth_guild
		}
	}

	illustration_tags = {
		10 = regular
		10 = exterior
	}

	immediate = {
		ruler_or_regent ?= {
			save_scope_as = target_character
		}

		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }
	}

	option = {
		name = flavor_luc.2.a
		
		location:lucca = {
			add_location_modifier = {
				mode = add_and_extend
				modifier = luc_fine_cloth_productions
			}

			market = {
				add_temporary_demand = { type = demand:luc_fine_cloth_demand months = 240 }
			}
		}
	}
}

#Merchants in London
flavor_luc.3 = {
	type = country_event
	title = flavor_luc.3.title
	desc = flavor_luc.3.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = LUC
		from = 1350.1.1
		to = 1500.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		location:london.top_owner = {
			this != root
			NOT = { is_at_war_with = root }
			within_diplomatic_range = root
		}
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:burghers_estate background = estate_type:burghers_estate }

		location:london.top_owner = {
			save_scope_as = target_country
		}

		location:london.market = {
			save_scope_as = target_market
		}
	}

	option = {
		name = flavor_luc.3.a
		
		location:london = {
			construct_building = {
				building_type = building_type:trade_office
				cost_multiplier = 0.25
				cost_multiplier_reason = "burghers_cheap_sponsorship"
				owner = root
				payer = root
			}
		}
	}

	option = {
		name = flavor_luc.3.b
		
		add_opinion_mutual_effect = {
			modifier = luc_did_not_intervene_in_trade
			target = scope:target_country
		}

		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_move_to_right
		}
	}
}

#Libertas! Event
flavor_luc.4 = {
	type = country_event
	title = flavor_luc.4.title
	desc = flavor_luc.4.desc
	outcome = neutral
	
	dynamic_historical_event = {
		tag = LUC
		from = 1400.1.1
		to = 1500.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		at_war = no

		owns = location:lucca
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:burghers_estate }
	}

	option = { #Defensive Option. Law
		name = flavor_luc.4.a
		
		unlock_government_reform_effect = {
			type = luc_libertas
		}
	}

	option = { #Offensive Option. Less Major
		name = flavor_luc.4.b
		
		change_societal_value = {
			type = offensive_vs_defensive
			value = societal_value_move_to_left
		}

		add_country_modifier = {
			mode = add
			modifier = luc_expansion_of_borders
			years = 20
		}
	}
}

#Great Walls of Lucca
flavor_luc.5 = {
	type = country_event
	title = flavor_luc.5.title
	desc = flavor_luc.5.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = LUC
		from = 1450.1.1
		to = 1600.1.1
		monthly_chance = 2
	}

	fire_only_once = yes

	trigger = {
		owns = location:lucca
		
		location:lucca = {
			has_building = building_type:city_walls
		}
	}

	immediate = {

	}

	option = {
		name = flavor_luc.5.a
		
		location:lucca = {
			construct_building = {
				building_type = building_type:walls_of_lucca
				cost_multiplier = 1
			}
		}

		change_societal_value = {
			type = offensive_vs_defensive
			value = societal_value_move_to_right
		}
	}

	option = {
		name = flavor_luc.5.b

		change_societal_value = {
			type = offensive_vs_defensive
			value = societal_value_move_to_left
		}
		
	}
}

#Propose to be sold to Pisa. Pisa is easier to break free from
flavor_luc.1000 = {
	type = country_event
	title = flavor_luc.1000.title
	desc = flavor_luc.1000.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = LUC
		from = 1337.1.1
		to = 1400.1.1
		monthly_chance = 20 #This should be really likely to happen
	}

	fire_only_once = yes

	trigger = {
		is_subject = yes
		NOT = {
			is_subject_of = c:PIS
		}

		country_exists = c:PIS

		overlord = {
			at_war = yes
			NOT = { is_at_war_with = c:PIS }
		}
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:burghers_estate }

		c:PIS ?= {
			ruler_or_regent ?= {
				save_scope_as = target_character
			}
		}

		overlord = {
			save_scope_as = target_country
			ruler_or_regent ?= {
				save_scope_as = target_character2
			}
		}

		save_scope_as = from_country
	}

	option = { #Suggest to overlord for the purchase
		name = flavor_luc.1000.a
		
		overlord = {
			trigger_event_non_silently = {
				id = flavor_luc.1001
			}
		}
	}

	option = { #No need to do that. We have better odds with the current overlord
		name = flavor_luc.1000.b
		
		add_prestige = prestige_mild_bonus
	}
}

#Buy Lucca
flavor_luc.1001 = {
	type = country_event
	title = flavor_luc.1001.title
	desc = flavor_luc.1001.desc
	outcome = neutral

	major = yes
	major_trigger = {
		this = c:LUC	
	}

	immediate = {
		
		ordered_known_country = {
			order_by = military_strength
			limit = {
				is_at_war_with = root
			}

			max = 1
			check_range_bounds = yes
			save_scope_as = enemy_country
		}
	}

	option = {
		name = flavor_luc.1001.a
		
		c:PIS ?= {
			trigger_event_non_silently = {
				id = flavor_luc.1002
			}
		}
	}

	option = {
		name = flavor_luc.1001.b
		
		add_opinion_mutual_effect = {
			modifier = luc_refused_to_sell_us
			target = scope:from_country
		}
	}
}

#Buy Lucca - Buyer Version
flavor_luc.1002 = {
	type = country_event
	title = flavor_luc.1002.title
	desc = flavor_luc.1002.desc
	major = yes
	major_trigger = {
		OR = {
			this = scope:target_country
			this = c:LUC	
		}	
	}
	outcome = neutral

	option = {
		name = flavor_luc.1002.a
		
		transfer_yearly_gold = {
			target = scope:target_country
			value = 2
		}
		scope:from_country = {
			make_subject_of = {
				target = root
				type = subject_type:vassal
			}
		}
	}

	option = {
		name = flavor_luc.1002.b
		
		add_opinion_mutual_effect = {
			modifier = luc_refused_to_buy_our_subject
			target = scope:target_country
		}
	}
}

#Alliance with Genoa
flavor_luc.1100 = {
	type = country_event
	title = flavor_luc.1100.title
	desc = flavor_luc.1100.desc
	outcome = neutral

	dynamic_historical_event = {
		tag = LUC
		from = 1370.1.1
		to = 1450.1.1
		monthly_chance = 3
	}

	fire_only_once = yes

	trigger = {
		country_exists = c:GEN

		is_subject = no
		c:GEN = { is_subject = no }

		NOT = {
			is_at_war_with = c:GEN
		}
	}

	immediate = {
		
		c:GEN = {
			ruler_or_regent ?= {
				save_scope_as = target_character
			}
		}

		ruler_or_regent ?= {
			save_scope_as = target_character2
		}

		save_scope_as = from_country
	}

	option = {
		name = flavor_luc.1100.a
		
		c:GEN = {
			trigger_event_non_silently = {
				id = flavor_luc.1101
			}
		}

		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_move_to_right
		}
	}

	option = {
		name = flavor_luc.1100.b
		
		change_societal_value = {
			type = belligerent_vs_conciliatory
			value = societal_value_move_to_left
		}
	}
}

flavor_luc.1101 = {
	type = country_event
	title = flavor_luc.1101.title
	desc = flavor_luc.1101.desc
	outcome = neutral

	option = {
		name = flavor_luc.1101.a
		
		create_alliance = {
			target = scope:from_country
		}
	}

	option = {
		name = flavor_luc.1101.b
		
		add_opinion_mutual_effect = {
			modifier = luc_refused_alliance
			target = scope:from_country
		}
	}
}