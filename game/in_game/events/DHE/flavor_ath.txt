namespace = flavor_ath

flavor_ath.1 = {
	type = country_event
	title = flavor_ath.1.title
	desc = flavor_ath.1.desc
	outcome = neutral

	illustration_tags = {
		10 = exterior
		10 = angry
	}

	immediate = {
		event_illustration_estate_effect = {
			foreground = estate_type:peasants_estate
			background = estate_type:peasants_estate
		}
		c:AYD = {
			save_scope_as = target_country
		}
		character:ayd_muhammad = {
			save_scope_as = target_character
		}
		character:ayd_umur = {
			save_scope_as = target_character2
		}
	}

	option = {
		name = flavor_ath.1.a
		
		add_casus_belli = {
			target = c:AYD
			type = casus_belli:cb_anti_piracy
		}

		add_opinion = {
			modifier = opinion_raided_our_borders
			target = c:AYD
		}
	}
}
flavor_ath.2 = { # Sudden Death of the Duke Guillem II
    type = country_event
    title = flavor_ath.2.title
    desc = flavor_ath.2.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        from = 1338.1.1 
        to = 1342.1.1 
        monthly_chance = 60
    }


    trigger = {
        has_ruler = yes
        is_subject = yes
        has_heir = yes
        country_exists = c:ARG
        country_exists = c:SIC
        overlord = c:SIC
        character:ath_william = {
            is_alive = yes
            is_ruler = yes
        }
        c:ARG = {
            has_ruler = yes
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        root = {
            save_scope_as = scope_athens
        }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        heir ?= {
            save_scope_as = scope_heir
        }
        if = {
            limit = {
                any_character = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:catalan
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
            }
            random_character = {
                limit = {
                    NOT = { is_child_of = root.ruler }
                    NOT = { is_sibling_of = root.ruler }
                    is_courtier = yes
                    is_female = no
                    is_alive = yes
                    is_adult = yes
                    culture = culture:catalan
                    religion = religion:catholic
                    has_estate = estate_type:nobles_estate
                }
                save_scope_as = regent_of_athens
                set_variable = ath_regent_of_athens
            }
        }
        else = {
            create_character = {
                adm = { 50 70 }
                dip = { 40 63 }
                mil = { 50 74 }
                age = { 45 49 }
                culture = culture:catalan
                religion = religion:catholic
                estate = estate_type:nobles_estate
                save_scope_as = regent_of_athens
                set_variable = ath_regent_of_athens
                add_character_modifier = { modifier = block_marriage years = 20 mode = add_and_extend }
            }
        }         
        scope:scope_heir = {
            move_country = c:SIC
        }
        c:SIC ?= {
            save_scope_as = sic_country
        }
        c:ARG ?= {
            save_scope_as = arg_country
            ruler ?= {
                save_scope_as = arg_ruler
            }
            save_scope_as = argos_scope
        }
    }
    

    option = { # Catalan rule must continue with <Sicily heir>
        name = flavor_ath.2.a    
        historical_option = yes  
        kill_character = {
            target = scope:scope_ruler
            reason = disease
        }
        add_stability = stability_mild_bonus
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_bonus }      
        ai_chance = {
            factor = 10
            modifier = {
                factor = 0.10
                C:ARG ?= {
                    is_human = yes
                }
            }
        }        
    }

    option = { # Invite the de Brienne to rule
        name = flavor_ath.2.b
        kill_character = {
            target = scope:scope_ruler
            reason = disease
        }
        add_legitimacy = legitimacy_mild_penalty
        ai_chance = {
            factor = 1
            modifier = {
                factor = 10
                C:ARG ?= {
                    is_human = yes
                }
            }
        }        
        custom_tooltip = ath_sicily_may_declare_war
        custom_tooltip = ath_legal_heir_of_guillem
        scope:argos_scope = { trigger_event_non_silently = { id = flavor_ath.3 days = 3 } } 
    }    
}

flavor_ath.3 = { # Argos Receives Athens Nobles Proposal 
    type = country_event
    title = flavor_ath.3.title
    desc = flavor_ath.3.desc
    outcome = neutral

    fire_only_once = yes

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = arg_ruler
        }
        character:ath_local_regent = {
            save_scope_as = regent_of_athens
        }
        character:ath_gautier_v_brienne = {
            save_scope_as = scope_slained_brienne
        }
        character:ath_william = {
            save_scope_as = scope_ath_william
        }
        c:ATH ?= {
            random_character = {
                limit = { has_variable = ath_regent_of_athens }
                save_scope_as = regent_of_athens
            }
            save_scope_as = scope_athens
        }
        c:SIC ?= {
            save_scope_as = scope_sic_country
        }
    }
    

    option = { # For Ai Argos # Accept
        name = flavor_ath.3.a    
        historical_option = yes
        trigger = {
            is_human = no
        }       
        show_as_tooltip = {
             every_owned_location = {
                limit = {
                    is_core_of = root
                }
                add_core = c:ATH
            }       
            c:ATH ?= {
                annex_country_diplomatically = {
                    country = root
                } 
            }              
        } 
        c:ATH ?= {
            trigger_event_non_silently = { id = flavor_ath.4 days = 1 } 
        }
        ai_chance = {
            base = 75
        }         
    }

    option = { # For Ai Argos # Decline
        name = flavor_ath.3.b    
        historical_option = yes 
        trigger = {
            is_human = no
        }         
        ai_chance = {
            base = 25
        } 
        c:ATH ?= {
            trigger_event_non_silently = { id = flavor_ath.7 days = 1 } 
        }             
    }    

    option = { # For Player Argos # Accept
        name = flavor_ath.3.a
        trigger = {
            is_human = yes
        }   
        custom_tooltip = {
            text = flavor_ath.3_become_athens
            annex_country_diplomatically = {
                country = c:ATH
            }
            change_country_name = ATH
            change_country_adjective = ATH_ADJ
            change_country_flag = ATH
            c:ATH ?= {
                every_owned_location = {
                    limit = {
                        is_core_of = c:ATH
                        NOT = { is_core_of = c:ARG }
                    }
                    add_core = root
                    remove_core = c:ATH
                } 
            } 
        }
        if = {
            limit = { country_rank_level < 2 }
            set_country_rank_effect = { rank = country_rank:rank_duchy }
        }  
        if = {
            limit = { 
                NOT = { 
                    capital = location:thebes
                } 
            }
            set_capital = location:thebes
        }  
        set_variable = { # This will enable Brienne events for the ARG tag as 'Athens'
            name = de_brienne_rule_of_athens
            value = 1
            years = 300
        }
        scope:regent_of_athens = {
            move_country = root
        }
        custom_tooltip = ath_arg_sicily_may_declare_war 
        trigger_event_non_silently = { id = flavor_ath.5 days = 1 } 
        scope:scope_sic_country = { trigger_event_non_silently = { id = flavor_ath.6 days = 3 } }        
    }

    option = { # For Player Argos # Decline
        name = flavor_ath.3.b
        add_prestige = prestige_mild_bonus
        trigger = {
            is_human = yes
        } 
        c:ATH ?= {
            trigger_event_non_silently = { id = flavor_ath.7 days = 1 } 
        }         
    }    
}

flavor_ath.4 = { # Count of Argos Accepts 
    type = country_event
    title = flavor_ath.4.title
    desc = flavor_ath.4.desc
    outcome = neutral

    fire_only_once = yes


    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {  
        save_scope_as = scope_athens
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        random_character = {
            limit = { has_variable = ath_regent_of_athens }
            save_scope_as = regent_of_athens
        }
        character:ath_william = {
            save_scope_as = scope_ath_william
        }
        c:ARG ?= {
            save_scope_as = arg_country_scope
            ruler ?= {
                save_scope_as = arg_ruler
            }
        }
        c:SIC ?= {
            save_scope_as = sic_country_scope
        }
        character:ath_gautier_v_brienne = {
            save_scope_as = scope_slained_brienne
        }
        if = {
            limit = { 
                is_subject = yes
            }
            overlord = { save_scope_as = ath_overlord }
        }
        set_variable = { # This will enable Brienne events for the ARG tag as 'Athens'
            name = de_brienne_rule_of_athens
            value = 1
            years = 300
        }
    }
    

    option = { # The Catalan Company will now need to be dealt with.
        name = flavor_ath.4.a
        scope:sic_country_scope = { trigger_event_non_silently = { id = flavor_ath.6 days = 3 } } 
        change_culture = culture:champenais
        c:ARG ?= {
              every_owned_location = {
                limit = {
                    is_core_of = root
                }
                add_core = c:ATH
            }           
        }      
        annex_country_diplomatically = {
            country = c:ARG
        }   
        scope:arg_ruler = {
            move_country = root
        }  
        set_new_ruler_with_union_if_senior = { character = scope:arg_ruler } 
        if = {
            limit = { exists = scope:ath_overlord }
            scope:ath_overlord = { cancel_subject = root }
        }     
        trigger_event_non_silently = { id = flavor_ath.5 }
        ai_chance = {
            base = 95
        }            
    }
}

flavor_ath.5 = { # Appeasement of the Catalan Company
    type = country_event
    title = flavor_ath.5.title
    desc = flavor_ath.5.desc
    outcome = neutral

    fire_only_once = yes

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:SIC ?= {
            save_scope_as = sic_country_scope
        }
        character:ath_william = {
            save_scope_as = scope_ath_william
        }
        random_character = {
            limit = { has_variable = ath_regent_of_athens }
            save_scope_as = regent_of_athens
        }
    }
    

    option = { # Grant them a greater portion of the Duchy's taxation.
        name = flavor_ath.5.a        
        ai_chance = {
            base = 25
        }   
        if = {
            limit = { NOT = { has_reform = government_reform:ath_rule_of_the_catalans } }
            add_reform = government_reform:ath_rule_of_the_catalans
        } 
        add_prestige = prestige_mild_penalty
        add_country_modifier = { modifier = ath_tax_catalan_appeasement years = 5 mode = add_and_extend }  
    }

    option = { # Expand the rights of those within the nobility
        name = flavor_ath.5.b        
        ai_chance = {
            base = 60
        } 
        if = {
            limit = { NOT = { has_reform = government_reform:ath_rule_of_the_catalans } }
            add_reform = government_reform:ath_rule_of_the_catalans
        } 
        add_legitimacy = legitimacy_mild_penalty
        add_country_modifier = { modifier = ath_estate_power_catalan_appeasement years = 10 mode = add_and_extend }            
    }

    option = { # They must be expelled from Athens!
        name = flavor_ath.5.c        
        ai_chance = {
            base = 15
        } 
        if = {
            limit = { has_reform = government_reform:ath_rule_of_the_catalans }
            remove_reform = government_reform:ath_rule_of_the_catalans
        }
        add_stability = stability_severe_penalty
        add_country_modifier = { modifier = ath_expulsion_of_the_catalan_company years = 10 mode = add_and_extend } 
        if = {
            limit = {
                has_country_modifier = ath_encroached_rule_of_the_catalan_company
            }
            remove_country_modifier = ath_encroached_rule_of_the_catalan_company
        }
        if = {
            limit = {
                has_country_modifier = ath_wary_of_the_catalan_company
            }
            remove_country_modifier = ath_wary_of_the_catalan_company
        }
        set_variable = {
            name = ath_catalan_company_expelled
            value = 1
            years = 500
        }                 
    }        
}

flavor_ath.6 = { # For Sicily ## Athens becomes independent
    type = country_event
    title = flavor_ath.6.title
    desc = flavor_ath.6.desc
    outcome = neutral

    fire_only_once = yes

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate } 
        ruler ?= {
            save_scope_as = scope_ruler
        }   
        c:ARG ?= {
            ruler ?= {
                save_scope_as = arg_ruler
            }
        }
        character:ath_william = {
            save_scope_as = scope_ath_william
        }
        if = {
            limit = { 
                AND = {
                   c:ARG = {
                        has_variable = de_brienne_rule_of_athens
                    }
                    NOT = { country_exists = c:ATH } 
                }
            }
            c:ARG ?= {
                save_scope_as = scope_athens
                ruler ?= {
                    save_scope_as = arg_ruler
                }
            }
        }
        if = {
            limit = { 
                country_exists = c:ATH  
            }
            c:ATH ?= {
                save_scope_as = scope_athens
                ruler ?= {
                    save_scope_as = arg_ruler
                }
            }
        }        
    }
    

    option = { # Damn them! ## If Argos is 'Athens'
        name = flavor_ath.6.a 
        trigger = {
            AND = {
                c:ARG = {
                    has_variable = de_brienne_rule_of_athens
                }
                NOT = { country_exists = c:ATH }
            }            
        }
        add_casus_belli =  { 
            target = c:ARG
            type = casus_belli:cb_subject_broke_free	
        }        
    }

    option = { # Damn them! ## If Athens 'tag' exists
        name = flavor_ath.6.a  
        trigger = {
            country_exists = c:ATH           
        }
        add_casus_belli =  { 
            target = c:ATH
            type = casus_belli:cb_subject_broke_free	
        }        
    }    
}

flavor_ath.7 = { # Duke of Argos declines
    type = country_event
    title = flavor_ath.7.title
    desc = flavor_ath.7.desc
    outcome = neutral

    fire_only_once = yes

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        heir ?= {
            save_scope_as = scope_heir
        }
        random_character = {
            limit = { has_variable = ath_regent_of_athens }
            save_scope_as = regent_of_athens
        }
        c:ARG ?= {
            save_scope_as = arg_country
            ruler ?= {
                save_scope_as = arg_ruler
            }
        }
    }
    

    option = { # Curses
        name = flavor_ath.7.a           
        ai_chance = {
            base = 95
        }   
        add_prestige = prestige_severe_penalty
    }
    option = { # Imprison him!
        name = flavor_ath.7.b          
        ai_chance = {
            base = 95
        }   
        add_prestige = prestige_severe_penalty
        scope:regent_of_athens = {
            add_character_modifier = { modifier = imprisoned years = 25 mode = add_and_extend }
        }
    }    
}

flavor_ath.8 = { # Rule of the Catalan Company 
    type = country_event
    title = flavor_ath.8.title
    desc = flavor_ath.8.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        from = 1337.5.1 
        to = 1382.1.1 
        monthly_chance = 100
    }


    trigger = {
        at_war = no      
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:SIC ?= {
            save_scope_as = sic_country
        }
        c:ARG ?= {
            ruler ?= {
                save_scope_as = arg_ruler
            }
            save_scope_as = arg_country
        }
    }
    

    option = { # May the members of the company continue to serve us well.
        name = flavor_ath.8.a    
        historical_option = yes   
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_bonus }
        add_country_modifier = { modifier = ath_encroached_rule_of_the_catalan_company years = 25 mode = add_and_extend }     
        ai_chance = {
            base = 70
        }            
    }

    option = { # We should remain wary of the company
        name = flavor_ath.8.b
        add_stability = stability_mild_bonus
        add_country_modifier = { modifier = ath_wary_of_the_catalan_company years = 25 mode = add_and_extend }
        ai_chance = {
            base = 30
        }              
    }
}

flavor_ath.9 = { # Byzantium Internal Strife
    type = country_event
    title = flavor_ath.9.title
    desc = flavor_ath.9.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1342.1.1 
        to = 1382.1.1 
        monthly_chance = 18
    }


    trigger = {
        at_war = no
        country_exists = c:BYZ 
        c:BYZ ?= {
            in_civil_war = no
            stability < -15
        }
        has_reform = government_reform:ath_rule_of_the_catalans
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:BYZ ?= {
            save_scope_as = byz_country
        }
    }
    

    option = { # Push to claim Thessaly
        name = flavor_ath.9.a  
        add_country_modifier = { modifier = ath_strike_into_thessaly years = 5 mode = add }  
        add_casus_belli = {
            target = c:BYZ
            type = casus_belli:cb_lat_reconquest
        }   
        ai_chance = {
            base = 95
        }            
    }

    option = { # Send in the Catalan Company to raid instead.
        name = flavor_ath.9.b
        trigger = {
            has_reform = government_reform:ath_rule_of_the_catalans
        }
        add_gold = {
            value = { 300 500 }
        } 
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = 2
        }
        reverse_add_opinion = { target = c:BYZ modifier = ath_byz_raided_our_greek_holdings }
        c:BYZ ?= {
            area:northern_greece_area = {
                every_province_in_area = {
                    limit = {
                        owner = c:BYZ
                    }
                    change_province_food_percentage = -0.33

                }
                every_location_in_area = {
                    limit = {
                        owner = c:BYZ
                    }
                    change_prosperity = prosperity_severe_penalty
                }    
            }
        }
        ai_chance = {
            base = 5
        }              
    }
    option = { # We shall wait for a better opportunity to strike the Byzantines!
        name = flavor_ath.9.c
        ai_chance = {
            base = 5
        }     
        add_stability = stability_mild_bonus
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = -1
        }
    }
}



flavor_ath.10 = { # Rule in Greece ## Relations with the local populace
    type = country_event
    title = flavor_ath.10.title
    desc = flavor_ath.10.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1344.1.1 
        to = 1370.1.1 
        monthly_chance = 10
    }


    trigger = {
        at_war = no
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }        
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:LAT ?= {
            save_scope_as = latin_country
        }
    }
    

    option = { # Maintain the influence of the elite.
        name = flavor_ath.10.a    
        historical_option = yes
        add_legitimacy = legitimacy_mild_bonus  
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_bonus }
        add_country_modifier = { modifier = ath_favored_latin_elite years = 10 mode = add_and_extend }
        custom_tooltip = {
            text = ath_greek_pops_lose_mild_satisfaction
            every_owned_location = {
                limit = {
                    any_pop = { culture = culture:greek_culture }
                }
                every_pop = {
                    limit = {
                        owner = root
                        culture = culture:greek_culture
                    }
                    add_pop_satisfaction = pop_satisfaction_mild_penalty
                }
            }
        }          
    }

    option = { # Placate the Greek populace
        name = flavor_ath.10.b  
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_mild_penalty }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_mild_bonus }
        add_country_modifier = { modifier = ath_placated_the_greek_populace years = 15 mode = add_and_extend }
        custom_tooltip = {
            text = ath_greek_pops_gain_severe_satisfaction
            every_owned_location = {
                limit = {
                    any_pop = { culture = culture:greek_culture }
                }
                every_pop = {
                    limit = {
                        owner = root
                        culture = culture:greek_culture
                    }
                    add_pop_satisfaction = pop_satisfaction_severe_bonus
                }
            }
        }  
        culture:greek_culture = {
            change_cultural_view = {
                target = root.culture
                change = 1
            }
        }                  
    }

    option = { # Bolster the Catalan privileges
        name = flavor_ath.10.c  
        trigger = {
            has_reform = government_reform:ath_rule_of_the_catalans
        } 
        add_legitimacy = legitimacy_weak_penalty  
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_bonus }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_mild_penalty }
        add_country_modifier = { modifier = ath_rewarded_catalans_greek_lands years = 15 mode = add_and_extend } 
        custom_tooltip = {
            text = ath_greek_pops_lose_severe_satisfaction
            every_owned_location = {
                limit = {
                    any_pop = { culture = culture:greek_culture }
                }
                every_pop = {
                    limit = {
                        owner = root
                        culture = culture:greek_culture
                    }
                    add_pop_satisfaction = estate_satisfaction_severe_penalty
                }
            }
        }  
        culture:greek_culture = {
            change_cultural_view = {
                target = root.culture
                change = -1
            }
        }                   
    }
    option = { # Maintain the current status quo
        name = flavor_ath.10.d
        add_prestige = prestige_weak_penalty
        add_legitimacy = legitimacy_weak_penalty
        add_stability = stability_weak_bonus
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_bonus }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_weak_penalty }            
    }
}


flavor_ath.11 = { # Conflict between the Catalan Company and Greeks in <location> 
    type = country_event
    title = flavor_ath.11.title
    desc = flavor_ath.11.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1340.1.1 
        to = 1440.1.1 
        monthly_chance = 2
    }


    trigger = {
        at_war = no
        has_reform = government_reform:ath_rule_of_the_catalans
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }
        any_owned_rural_location = {
            any_pop = {
                pop_type = pop_type:peasants
                culture = culture:greek_culture
            }
            count > 1
        }        
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        random_owned_rural_location = {
            limit = {
                any_pop = {
                    culture = culture:greek_culture
                }
            }
            save_scope_as = target_conflict_location
        }
    }
    

    option = { # Back the Catalans
        name = flavor_ath.11.a    
        historical_option = yes        
        ai_chance = {
            base = 95
        }  
        scope:target_conflict_location = {
            add_location_modifier = { modifier = ath_permitted_catalan_settlements years = 15 mode = add_and_extend } 
            every_pop = {
                limit = {
                    owner = root
                    pop_type = pop_type:peasants
                    culture = culture:greek_culture
                }
                add_pop_satisfaction = pop_satisfaction_ultimate_penalty
            }
        }          
    }

    option = { # Reprimand the Catalan Company
        name = flavor_ath.11.b
        ai_chance = {
            base = 5
        }  
        add_legitimacy = legitimacy_mild_bonus
        add_prestige = prestige_mild_penalty
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_weak_penalty }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_mild_bonus }
        scope:target_conflict_location = {
            every_pop = {
                limit = {
                    owner = root
                    pop_type = pop_type:peasants
                    culture = culture:greek_culture
                }
                add_pop_satisfaction = pop_satisfaction_mild_bonus
            }
        }             
    }
    option = { # Use the opportunity to exile the Catalans from <athens!>
        name = flavor_ath.11.c
        ai_chance = {
            base = 5
        }
        if = {
            limit = { has_reform = government_reform:ath_rule_of_the_catalans }
            remove_reform = government_reform:ath_rule_of_the_catalans
        }
        add_stability = stability_mild_penalty
        add_country_modifier = { modifier = ath_expulsion_of_the_catalan_company years = 5 mode = add_and_extend }
        if = {
            limit = {
                has_country_modifier = ath_encroached_rule_of_the_catalan_company
            }
            remove_country_modifier = ath_encroached_rule_of_the_catalan_company
        }
        if = {
            limit = {
                has_country_modifier = ath_wary_of_the_catalan_company
            }
            remove_country_modifier = ath_wary_of_the_catalan_company
        }
        add_estate_satisfaction = { type = estate_type:nobles_estate value = estate_satisfaction_severe_penalty }
        add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_severe_bonus } 
        set_variable = {
            name = ath_catalan_company_expelled
            value = 1
            years = 500
        }         
        scope:target_conflict_location = {
            every_pop = {
                limit = {
                    owner = root
                    pop_type = pop_type:peasants
                    culture = culture:greek_culture
                }
                add_pop_satisfaction = pop_satisfaction_ultimate_bonus
            }
        }                      
    }
}


flavor_ath.12 = { # The Shifting Capitals ## Catalan Company shifting base of operations in Athens
    type = country_event
    title = flavor_ath.12.title
    desc = flavor_ath.12.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1341.1.1 
        to = 1405.1.1 
        monthly_chance = 6
    }


    trigger = {
        at_war = no
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }  
        owns = location:athens
        owns = location:thebes      
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        location:athens = {
            save_scope_as = athens_location
        }
        location:thebes = {
            save_scope_as = thebes_location
        }
    }
    
     option = { # Both locations are invaluable, maintain the current structure.
        name = flavor_ath.12.a
        historical_option = yes
        add_stability = stability_mild_bonus
        location:athens = {
            add_location_modifier = {
                modifier = ath_benefits_of_the_shifting_capitals
                years = 30
                mode = add_and_extend
            }
        }
        location:thebes = {
            add_location_modifier = {
                modifier = ath_benefits_of_the_shifting_capitals
                years = 30
                mode = add_and_extend
            }
        }
        ai_chance = {
            factor = 0.25
        }
     }
     option = { # Athens should serve as our proper capital.
        name = flavor_ath.12.b
        add_prestige = prestige_mild_bonus
        location:athens = {
            change_development = development_mild_bonus
            add_location_modifier = {
                modifier = ath_a_permanent_capital
                years = 30
                mode = add
            }
            change_prosperity = prosperity_extreme_bonus
        }
        if = {
            limit = { 
                NOT = { 
                    capital = location:athens
                } 
            }
            set_capital = location:athens
        }
        ai_chance = {
            factor = 0.5
        }
     }

     option = { # Maintain the court in Thebes, it has served us well.
        name = flavor_ath.12.c
        add_legitimacy = legitimacy_mild_bonus
        location:thebes = {
            change_development = development_severe_bonus
            add_location_modifier = {
                modifier = ath_a_permanent_capital
                years = 30
                mode = add_and_extend
            }
            change_prosperity = prosperity_weak_bonus
        }
        if = {
            limit = { 
                NOT = { 
                    capital = location:thebes
                } 
            }
            set_capital = location:thebes
        }
        ai_chance = {
            factor = 0.15
        }
     }
}


flavor_ath.13 = { # Secession in Epirus ## Chance to strike Epirus
    type = country_event
    title = flavor_ath.13.title
    desc = flavor_ath.13.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1341.1.1 
        to = 1366.1.1 
        monthly_chance = 30
    }


    trigger = {
        at_war = no
        country_exists = c:BYZ
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }  
        country_exists = c:EPI
        character:epi_nikephoros_ii_orsini ?= {
            is_alive = yes
        }        
        c:EPI ?= {
            is_subject = no
            ruler ?= character:epi_nikephoros_ii_orsini
        }             
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        character:epi_theodoros_synadenos = {
            save_scope_as = scope_epi_governor
        }
        c:EPI ?= {
            save_scope_as = epi_country
            ruler ?= {
                save_scope_as = scope_epi_ruler
            }
        }
        c:BYZ ?= {
            save_scope_as = byz_country
        }
    }
    

    option = { # Now is the timne to strike!
        name = flavor_ath.13.a    
        historical_option = yes        
        ai_chance = {
            base = 95
        }    
        add_casus_belli = {
            target = c:EPI
            type = casus_belli:cb_lat_reconquest
        }          
    }

    option = { # Let's strive to improve relations instead
        name = flavor_ath.13.b
        ai_chance = {
            base = 5
        }   
        add_opinion = { target = c:EPI modifier = ath_befriend_epirus }
        reverse_add_opinion = { target = c:EPI modifier = ath_befriend_epirus }       
    }
    option = { # Epirus affairs are not our concern.
        name = flavor_ath.13.c
        add_stability = stability_mild_bonus
        add_prestige = prestige_weak_penalty
        ai_chance = {
            base = 5
        }              
    }
}

flavor_ath.14 = { # The Titular Emperor in Achaea
    type = country_event
    title = flavor_ath.14.title
    desc = flavor_ath.14.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1339.1.1 
        to = 1390.1.1 
        monthly_chance = 8
    }


    trigger = {
        at_war = no
        country_exists = c:ACH
        country_exists = c:NAP
        c:ACH ?= {
            has_reform = government_reform:ach_heirs_of_the_latin_empire
            has_ruler = yes
        }
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
        }      
        NOT = { country_exists = c:LAT }  
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:ACH ?= {
            save_scope_as = scope_ach_country
            ruler ?= {
                save_scope_as = scope_ach_ruler
            }
        }
        c:NAP ?= {
            save_scope_as = scope_nap_country
        }
    }
    

    option = { # We should invade Achaea and further weaken their claim to the Latin empire.
        name = flavor_ath.14.a    
        historical_option = yes 
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = 0.5
        } 
        add_casus_belli = {
            target = c:ACH
            type = casus_belli:cb_lat_reconquest
        }       
        ai_chance = {
            base = 95
        }            
    }

    option = { # We should instead seek to curry relations with the <ach_ruler>
        name = flavor_ath.14.b
        add_legitimacy = legitimacy_mild_bonus
        add_opinion = { target = c:ACH modifier = ath_acknowledged_the_latin_claim }
        c:ACH = {
            add_opinion = { target = ROOT modifier = ath_acknowledged_the_latin_claim }
        }          
        ai_chance = {
            base = 5
        }              
    }
    option = { # The affairs of the Titular rulers does not concern <athens>
        name = flavor_ath.14.c
        add_prestige = prestige_severe_bonus        
        ai_chance = {
            base = 5
        }              
    }
}

flavor_ath.15 = { # Pope Proposes Restoring the Latin empire ## Athens/Argos Restoration Gov reform ## Simliar to Achaea
    type = country_event
    title = flavor_ath.15.title
    desc = flavor_ath.15.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1337.1.1 
        to = 1600.1.1 
        monthly_chance = 50
    }


    trigger = {
        NOT = { has_reform = government_reform:ach_heirs_of_the_latin_empire } 
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        } 
        scripted_geography:frankokratia_geography= { # Retool to be for Frankokratia region
            any_location_in_scripted_geography = {
                owner ?= ROOT
                count > 32
            }
        }  
        country_exists = c:PAP
        country_exists = c:BYZ
        c:PAP ?= {
            has_ruler = yes
        }   
        NOT = { country_exists = c:LAT } 
        religion = religion:catholic
        NOT = { num_locations >= 70 }
        NOT = { owns = location:constantinople }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:PAP ?= {
          save_scope_as = scope_papal_states
          ruler ?= {
                save_scope_as = the_pope
            }  
        }
        c:BYZ ?= {
          save_scope_as = scope_byz
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }
    }
    

    option = { # Accept the Pope offer to restore the Latin empire
        name = flavor_ath.15.a 
        add_legitimacy = legitimacy_extreme_bonus
        unlock_government_reform_effect = { type = lat_papal_restoration_claimant }
        add_reform = government_reform:lat_papal_restoration_claimant
        add_country_modifier = { modifier = lat_support_from_the_papacy years = 25 mode = add } 
        add_gold = {
            value = c:PAP.monthly_income_trade_and_tax
            multiply = 4
            min = 250
            max = 1200
        } 
        set_variable = {
            name = lat_accepted_pope_latin_empire_proposal
            value = 1
            years = 100
        }  
        add_opinion = { target = c:PAP modifier = ath_papal_latin_restoration }
        c:PAP = {
            add_opinion = { target = c:ATH modifier = ath_papal_latin_restoration }
        } 
    }
  
    option = { # Reject his offer, we shall go our own path
        name = flavor_ath.15.b
        add_prestige = prestige_severe_bonus
        add_legitimacy = legitimacy_mild_penalty
        c:PAP = {
            add_opinion = { target = c:ATH modifier = ath_declined_papal_latin_restoration }
        }        
    }
}


flavor_ath.16 = { # Reclamation of Thessalonican lands
    type = country_event
    title = flavor_ath.16.title
    desc = flavor_ath.16.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        tag = ALB 
        tag = BOD
        tag = CEP
        tag = CRT
        tag = KNI  
        tag = NAX
        tag = NEG
        tag = NEO
        tag = PAT
        tag = SAO           
        from = 1337.1.1 
        to = 1600.1.1 
        monthly_chance = 33
    }


    trigger = {
        at_war = no 
        scripted_geography:claims_of_thessalonica_geography= { # Retool to be for Frankokratia region
            any_location_in_scripted_geography = {
                owner ?= ROOT
                count > 10
            }
        }  
        NOT = { country_exists = c:THE }
        NOT = { country_exists = c:LAT }
        country_exists = c:ACH
        is_frankokratia_state = yes  
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        character:the_theodoros_komnenos_doukas = {
            save_scope_as = scope_theodoros_doukas
        }
        c:ACH ?= {
            save_scope_as = achaea_country
        }
    }
    

    option = { # Claim the title from Naples
        name = flavor_ath.16.a    
        add_prestige = prestige_severe_bonus
        add_legitimacy = legitimacy_weak_penalty
        custom_tooltip = may_form_thessalonica
        set_variable = {
            name = halved_cost_to_form_thesslonica
            value = 1
            years = 300
        }
        c:ACH = {
            add_opinion = { target = c:ATH modifier = ath_claimed_thessalonica }
        }
        if = {
            limit = {
                owns = location:thessaloniki
                NOT = { capital = location:thessaloniki }
            }
            set_capital = location:thessaloniki
        }
        ai_chance = {
            base = 95
        }
    }

    option = { # Proclaim a new kingdom in its place.
        name = flavor_ath.16.b
        add_prestige = prestige_extreme_bonus
        if = {
            limit = { country_rank_level < 3 }
            set_country_rank_effect = { rank = country_rank:rank_kingdom }
        }
        c:ACH = {
            add_opinion = { target = c:ATH modifier = ath_proclaimed_rival_thessalonica_kingdom }
        }  
        ai_chance = {
            base = 5
        }              
    }
    option = { # We shouldn't lay claim to contested titles of the past.
        name = flavor_ath.16.c
        add_army_tradition = army_tradition_extreme_bonus
        ai_chance = {
            base = 5
        }              
    }
}

flavor_ath.17 = { # Constantinople Conquered!
    type = country_event
    title = flavor_ath.17.title
    desc = flavor_ath.17.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = ATH
        tag = ARG
        from = 1337.1.1 
        to = 1537.1.1 
        monthly_chance = 100
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        NOT = { has_reform = government_reform:ach_heirs_of_the_latin_empire } 
        trigger_if = {
            limit = {
                tag = ARG 
                custom_tooltip = {
                	text = variable_de_brienne_rule_of_athens_tt
                	has_variable = de_brienne_rule_of_athens
                }
            }
            NOT = { country_exists = c:ATH }
        }  
        NOT = { country_exists = c:LAT }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:PAP ?= {
          save_scope_as = scope_papal_states
          ruler ?= {
                save_scope_as = the_pope
            }  
        }
        c:BYZ ?= {
          save_scope_as = scope_byz
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }        
    }
    

    option = { # We shall claim the mantle of old Latin Emperors
        name = flavor_ath.17.a 
        add_prestige = prestige_radical_bonus
        form_country = formable_country:LAT_f
        if = {
            limit = { 
                NOT = { 
                    capital = location:constantinople
                } 
            }
            set_capital = location:constantinople
        }
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        }
        if = {
            limit = { country_rank_level < 4 }
            set_country_rank_effect = { rank = country_rank:rank_empire }
        }
        if = {
            limit = { has_reform = government_reform:lat_papal_restoration_claimant }
            remove_reform = government_reform:lat_papal_restoration_claimant
        }
        if = {
            limit = { has_reform = government_reform:ach_heirs_of_the_latin_empire }
            remove_reform = government_reform:ach_heirs_of_the_latin_empire
        }
        if = {
            limit = { has_reform = government_reform:partitio_reform }
            remove_reform = government_reform:partitio_reform
        }
        unlock_government_reform_effect = { type = lat_restored_rule_of_the_latins }
        add_reform = government_reform:lat_restored_rule_of_the_latins
		if = {
			limit = { 
				culture = culture:catalan
				NOT = { has_variable = de_brienne_rule_of_athens }
				NOT = { has_reform = government_reform:ach_heirs_of_the_latin_empire } 
			}
			hidden_effect = {
				change_country_color = map_LAT_catalan_restoration
			}
		}
		if = {
			limit = { 
				has_reform = government_reform:ach_heirs_of_the_latin_empire
				NOT = { has_variable = de_brienne_rule_of_athens }
			}
			hidden_effect = {
				change_country_color = map_LAT_titular_restoration
			}            
		}
		else_if = {
			limit = {
				NOT = { has_reform = government_reform:ach_heirs_of_the_latin_empire } 
				NOT = { culture = culture:catalan }
			}
			hidden_effect = {
				change_country_color = map_LAT
			} 
		}
    }
  
    option = { # Aim to stabilize our position in greece first.
        name = flavor_ath.17.b
        add_stability = stability_very_extreme_bonus
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        }    
        set_variable = {
            name = lat_reduced_requirements_to_form
            value = 1
            years = 300
        }
        custom_tooltip = latin_empire_reformation_conditions_unchanged_tt
    }

    option = { #  Let us instead continue to rule as <king> of Thessalonica 
        name = flavor_ath.17.c
        add_legitimacy = legitimacy_ultimate_bonus 
        add_prestige = prestige_ultimate_bonus
        if = {
            limit = {
                location:constantinople = {
                    NOT = { is_core_of = ROOT }
                }
            }
            location:constantinople = { add_core = ROOT }
        } 
    }
}
