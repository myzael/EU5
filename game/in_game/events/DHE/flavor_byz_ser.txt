namespace = flavor_byz_ser

scripted_effect append_info_if_SMC_is_player = {
	if = {
		limit = {
			c:SMC = {
				is_ai = no
			}
		}
		custom_tooltip = SMC_player
	}
}

flavor_byz_ser.1 = {
	type = country_event
	title = flavor_byz_ser.1.title
	desc = flavor_byz_ser.1.desc
	outcome = neutral

	fire_only_once = yes
	dynamic_historical_event = {
		tag = SER
		from = 1337.1.1
		to = 1337.12.1
		monthly_chance = 100
	}

	illustration_tags = {
		10 = exterior
	}

	trigger = {
		exists = c:SMC
		exists = c:BYZ
		exists = c:BUL
		exists = c:SMC.ruler
		c:SMC = { is_subject_or_below_of = ROOT }
	}

	immediate = {
		event_illustration_estate_background_effect = {
			background = estate_type:nobles_estate
		}
		c:SMC.ruler = {
			save_scope_as = target_character
		}
		c:SER = {
			save_scope_as = target_country
		}
		c:SMC = {
			save_scope_as = origin_country
		}
		c:BYZ = {
			save_scope_as = target_country2
		}
		c:BUL = {
			save_scope_as = target_country3
		}
		c:BYZ = {
			trigger_event_non_silently = {
				id = flavor_byz_ser.2
			}
		}
		c:SMC = {
			trigger_event_non_silently = {
				id = flavor_byz_ser.3
			}
		}
	}

	option = {
		name = flavor_byz_ser.1.b
		
		custom_tooltip = SER_threat_of_secession
		
		append_info_if_SMC_is_player = yes
	}


	option = {
		name = flavor_byz_ser.1.a
		
		custom_tooltip = SER_threat_of_secession

		change_societal_value = {
			type = centralization_vs_decentralization
			value = societal_value_move_to_left
		}

		scope:origin_country = {
			add_liberty_desire = liberty_desire_mild_plus
		}
	}
}

flavor_byz_ser.2 = {
	type = country_event
	title = flavor_byz_ser.2.title
	desc = flavor_byz_ser.2.desc
	outcome = neutral

	illustration_tags = {
		10 = exterior
	}

	option = {
		name = flavor_byz_ser.2.a

		add_country_modifier = {
			modifier = BYZ_underhanded_techniques
			years = 5
		}
		
		custom_tooltip = BYZ_threat_of_secession

		append_info_if_SMC_is_player = yes
	}

	after = {
		clear_saved_scope = target_character
		scope:target_country = {
			save_scope_as = target_rival
		}
		trigger_event_silently = {
			id = random_event.86 # A convenient "Offer from Shady People" event - rigged to target Serbia
			months = { 18 30 }
 		}
	}
}

flavor_byz_ser.3 = {
	type = country_event
	title = flavor_byz_ser.3.title
	desc = flavor_byz_ser.3.desc
	outcome = neutral

	illustration_tags = {
		10 = exterior
	}

	option = {
		name = flavor_byz_ser.3.a
		
		custom_tooltip = SMC_threat_of_secession
	}
}

flavor_byz_ser.4 = {
	type = country_event
	title = flavor_byz_ser.4.title
	desc = flavor_byz_ser.4.desc
	outcome = neutral

	fire_only_once = yes
	dynamic_historical_event = {
		tag = SMC
		from = 1341.1.1
		to = 1341.12.1
		monthly_chance = 100
	}

	illustration_tags = {
		10 = exterior
	}

	trigger = {
		country_exists = c:SER
		country_exists = c:BYZ
		is_subject_or_below_of = c:SER
	}

	immediate = {
		event_illustration_estate_background_effect = {
			background = estate_type:nobles_estate
		}
		save_scope_as = origin_country
		ruler_or_regent = {
			save_scope_as = target_character
		}
		c:SER = {
			save_scope_as = target_country
			ruler_or_regent ?= {
				save_scope_as = additional_character_for_portrait
			}
		}
		c:BYZ = {
			save_scope_as = target_country2
			ruler_or_regent ?= {
				save_scope_as = additional_character_for_portrait2
			}
		}
	}

	option = {
		name = flavor_byz_ser.4.a
		
		ai_will_select = {
			value = 100

			if = {
				limit = {
					subject_loyalty <= 50
				}
				value = 0
			}
		}

		add_liberty_desire = liberty_desire_mild_minus

		if = {
			limit = {
				subject_loyalty <= 50
			}
			add_stability = stability_severe_penalty
		}

		c:BYZ ?= {
			trigger_event_non_silently = {
				id = flavor_byz_ser.5
			}
		}

		c:SER ?= {
			trigger_event_non_silently = {
				id = flavor_byz_ser.5
			}
		}
	}

	option = {
		name = flavor_byz_ser.4.b
		
		ai_will_select = {
			value = 100

			if = {
				limit = {
					subject_loyalty > 50
				}
				value = 0
			}
		}

		if = {
			limit = {
				subject_loyalty > 50
			}
			add_stability = stability_severe_penalty
		}

		show_as_tooltip = {
			make_subject_of = {
				target = c:BYZ
				type = subject_type:vassal
			}
		}

		c:BYZ ?= {
			trigger_event_non_silently = {
				id = flavor_byz_ser.6
			}
		}

		c:SER ?= {
			trigger_event_non_silently = {
				id = flavor_byz_ser.6
			}
		}
	}
}

flavor_byz_ser.5 = {
	type = country_event
	title = flavor_byz_ser.5.title
	desc = flavor_byz_ser.5.desc
	outcome = neutral

	illustration_tags = {
		10 = exterior
	}

	option = {
		name = flavor_byz_ser.5.byz
		
		trigger = {
			tag = BYZ
		}

		custom_tooltip = SMC_remains_vassal_of_serbia

		add_prestige = prestige_mild_penalty
	}

	option = {
		name = flavor_byz_ser.5.ser
	
		trigger = {
			tag = SER
		}

		custom_tooltip = SMC_remains_vassal_of_serbia

		add_prestige = prestige_mild_bonus
		add_legitimacy = legitimacy_weak_bonus
	}
}

flavor_byz_ser.6 = {
	type = country_event
	title = flavor_byz_ser.6.title
	desc = flavor_byz_ser.6.desc
	outcome = neutral

	illustration_tags = {
		10 = exterior
	}
	
	option = {
		name = flavor_byz_ser.6.byz
		
		trigger = {
			tag = BYZ
		}

		scope:origin_country = {
			make_subject_of = {
				target = ROOT
				type = subject_type:vassal
			}
			if = {
				limit = {
					has_country_modifier = receiving_agitate_for_liberty
				}
				remove_country_modifier = receiving_agitate_for_liberty
			}
		}
		if = {
			limit = {
				has_scripted_relation = {
					target = scope:origin_country
					type = relation_type:agitate_for_liberty
				}
			}
			remove_relation = {
				first = ROOT
				second = scope:origin_country
				type = relation_type:agitate_for_liberty
			}
		}

		add_prestige = prestige_mild_bonus
		add_legitimacy = legitimacy_weak_bonus
	}

	option = {
		name = flavor_byz_ser.6.ser
	
		trigger = {
			tag = SER
		}

		show_as_tooltip = {
			scope:origin_country = {
				make_subject_of = {
					target = scope:target_country2
					type = subject_type:vassal
				}
			}
		}

		add_prestige = prestige_mild_penalty
	}
}
