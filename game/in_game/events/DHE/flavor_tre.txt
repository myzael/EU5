namespace = flavor_tre

#Expanding the Citadel of Trebizond
flavor_tre.1 = {
	hide_portraits = yes
	type = country_event
	title = flavor_tre.1.title
	desc = flavor_tre.1.desc
	outcome = neutral
	historical_info = flavor_tre.1.historical_info
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TRE
		from = 1370.1.1
		to = 1395.6.1
		monthly_chance = 1
	}
	
	trigger = {
		owns = location:trebizond
		at_war = no
		is_during_bankruptcy = no
	}

	illustration_tags = {
		10 = exterior
		10 = happy
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
		ruler_or_regent = { save_scope_as = target_character1 }
		location:trebizond = { save_scope_as = target_location }
	}

	option = {
		name = flavor_tre.1.a
		historical_option = yes

		change_gold_effect = { scale = -4 }
		scope:target_location = { change_development = development_mild_bonus change_prosperity = prosperity_weak_bonus }
	}
	option = {
		name = flavor_tre.1.b
		
		scope:target_location = { change_prosperity = prosperity_weak_penalty }
	}
}

flavor_tre.2 = {	#Scholarioi in Insubordination
	hide_portraits = yes
	type = country_event
	title = flavor_tre.2.title
	desc = flavor_tre.2.desc
	outcome = neutral
	historical_info = flavor_tre.2.historical_info
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1395.6.1
		monthly_chance = 1
	}
	
	trigger = {
		owns = location:trebizond
		at_war = no
		"estate_power(estate_type:nobles_estate)" > "estate_power(estate_type:crown_estate)"
		"estate(estate_type:nobles_estate)" = { num_privileges > 3 }
	}

	illustration_tags = {
		10 = interior
		10 = angry
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
		ruler_or_regent = { save_scope_as = target_character1 }
		random_estate_privilege = {
			limit = {
				estate_type = estate_type:nobles_estate
			}
			save_scope_as = first_privilege
		}
		random_estate_privilege = {
			limit = {
				estate_type = estate_type:nobles_estate
				this != scope:first_privilege
			}
			save_scope_as = second_privilege
		}
		random_estate_privilege = {
			limit = {
				estate_type = estate_type:nobles_estate
				this != scope:first_privilege
				this != scope:second_privilege
			}
			save_scope_as = third_privilege
		}
		location:trebizond = { save_scope_as = target_location }
	}

	option = {
		name = flavor_tre.2.a

		historical_option = yes

		add_stability = stability_ultimate_penalty
		if = {
			limit = { exists = scope:first_privilege }
			revoke_estate_privilege = scope:first_privilege
		}
		if = {
			limit = { exists = scope:second_privilege }
			revoke_estate_privilege = scope:second_privilege
		}
		if = {
			limit = { exists = scope:third_privilege }
			revoke_estate_privilege = scope:third_privilege
		}
		
	}
	
	option = {
		name = flavor_tre.2.b
		
	
		add_government_power = government_power_ultimate_penalty
	}
}

flavor_tre.3 = {	#Poisoning of Basil Megas Komnenos
	type = country_event
	title = flavor_tre.3.title
	desc = flavor_tre.3.desc
	outcome = neutral
	historical_info = flavor_tre.3.historical_info
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TRE
		from = 1339.1.1
		to = 1340.6.1
		monthly_chance = 2
	}
	
	trigger = {
		character:tre_basil_komnenos = { 
			owner = root
			is_alive = yes
			is_ruler = yes
			any_spouse = {
				this = character:tre_irene_palaiologos
				is_alive = yes
			}
		}
		at_war = no
	}

	illustration_tags = {
		10 = interior
		10 = angry
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
		ruler_or_regent = { save_scope_as = target_character1 }
		character:tre_irene_palaiologos = { save_scope_as = target_character2 }
		kill_character = {
			target = scope:target_character1
			reason = assassination
			killer = scope:target_character2
		}
	}

	option = {
		name = flavor_tre.3.a

		historical_option = yes

		set_new_ruler = scope:target_character2
		add_stability = stability_severe_penalty
		add_government_power = government_power_ultimate_penalty
	}

	option = {
		name = flavor_tre.3.b

		kill_character = {
			target = scope:target_character2
			reason = execution
		}
		if = { 
			limit = { 
				has_mutual_scripted_relation = {
					type = relation_type:alliance
					target = c:BYZ
				}
			}
			remove_relation = {
				first = ROOT
				second = c:BYZ
				type = relation_type:alliance
			}	
		}
		add_stability = stability_extreme_penalty
		add_government_power = government_power_ultimate_penalty
	}
}

flavor_tre.4 = { #The Reign of Alexios II
	type = country_event
	title = flavor_tre.4.title
	desc = flavor_tre.4.desc
	outcome = neutral
	fire_only_once = yes
	historical_info = flavor_TRE_4_historical_info

	image = "gfx/interface/illustrations/institutions/feudalism.dds"

	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 25
	}

	trigger = {
		has_ruler = yes
		ruler = character:tre_basil_komnenos
	}

	immediate = {
		ruler = { save_scope_as = target_character_1 }
		ruler.father = { save_scope_as = target_character_2 }
	}

	option = { #Rest now great Basileus
		name = flavor_tre.4.a
		
		scope:target_character_1 = { 
			add_character_modifier = {
				modifier = legacy_of_alexis_modifier
				years = -1
				mode = replace
			}
		}
	}
}

flavor_tre.5 = { #The Importance of Kerasous
	type = country_event
	title = flavor_tre.5.title
	desc = flavor_tre.5.desc
	outcome = neutral
	fire_only_once = yes
	historical_info = flavor_TRE_5_historical_info

	image = "gfx/interface/illustrations/trade_market/trade.dds"

	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 5
	}

	trigger = {
		owns = location:giresun
		at_war = no
		is_during_bankruptcy = no
	}

	immediate = {
		location:giresun = { save_scope_as = target_location }
	}

	option = { #Invest in Kerasous
		name = flavor_tre.5.a

		change_gold_effect = { scale = -4 }

		location:giresun = {
			upgrade_location_rank = yes
			add_location_modifier = {
				modifier = center_of_urbanization_modifier
				years = modifier_duration_years_long
				mode = replace
			}
		}
	}

	option = { #Let it prosper naturally
		name = flavor_tre.5.b

		location:giresun = {
			change_prosperity = prosperity_mild_bonus
		}
	}
}

flavor_tre.6 = { #Tax the Venetians and Genoese
	type = country_event
	title = flavor_tre.6.title
	desc = flavor_tre.6.desc
	outcome = neutral
	fire_only_once = yes

	image = "gfx/interface/illustrations/trade_market/trade.dds"

	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 2
	}

	trigger = {
		at_war = no
		is_subject = no
		AND = {
			country_exists = c:VEN
			c:VEN = { is_subject = no }
		}
		AND = {
			country_exists = c:GEN
			c:GEN = { is_subject = no }
		}
	}

	option = { #Impose new taxes
		name = flavor_tre.6.a

		add_country_modifier = {
			modifier = italian_merchant_taxes_modifier
			years = modifier_duration_years_long
			mode = replace
		}

		c:GEN ?= {
			add_opinion_mutual_effect = { target = root modifier = broken_trade_agreement }

			add_casus_belli = {
				target = root
				type = casus_belli:cb_insulted_us
				years = 20
			}
		}

		c:VEN ?= {
			add_opinion_mutual_effect = { target = root modifier = broken_trade_agreement }

			add_casus_belli = {
				target = root
				type = casus_belli:cb_insulted_us
				years = 20
			}
		}
	}

	option = { #Maintain privileges
		name = flavor_tre.6.b

		add_estate_satisfaction = { 
			type = estate_type:burghers_estate 
			value = estate_satisfaction_extreme_penalty 
		}

		c:GEN ?= {
			add_opinion_mutual_effect = { target = root modifier = honored_trade_agreement }

			add_trust_equilibrium = { 
				modifier = honored_trade_agreements
				target = root 
			}
		}

		c:VEN ?= {
			add_opinion_mutual_effect = { target = root modifier = honored_trade_agreement }

			add_trust_equilibrium = { 
				modifier = honored_trade_agreements
				target = root
			}
		}
	}
}

flavor_tre.8 = { #The Inheritance of Constantinople
	type = country_event
	title = flavor_tre.8.title
	desc = flavor_tre.8.desc
	outcome = positive
	fire_only_once = yes
	historical_info = flavor_tre.8.historical_info

	image = "gfx/interface/illustrations/government/throne_rooms/throne_room_byzantium.dds"

	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 25
	}

	trigger = {
		has_dlc = "d008_fate_of_the_phoenix"
		owns = location:constantinople
		has_ruler = yes
		ruler = { has_dynasty = yes }
	}

	immediate = {
		set_variable = tre_unlocked_byz_bureaucracies
		location:constantinople = { save_scope_as = target_location }
		ruler = { 
			save_scope_as = target_ruler 
			dynasty = { save_scope_as = target_dynasty }
		}
	}

	option = {
		name = flavor_tre.8.a

		custom_tooltip = {
			text = flavor_tre.8.a.tt
		}
	}
}

flavor_tre.7 = { #The Aspron
	type = country_event
	title = flavor_tre.7.title
	desc = flavor_tre.7.desc
	outcome = neutral
	fire_only_once = yes

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_silver.dds"

	dynamic_historical_event = {
		tag = TRE
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 1
	}

	trigger = {
		at_war = no
		is_during_bankruptcy = no
		owns = location:gumushane
	}

	immediate = {
		location:gumushane = { save_scope_as = target_location }
	}

	option = {
		name = flavor_tre.7.a

		add_government_power = government_power_extreme_penalty
		change_gold_effect = { scale = -1 }

		scope:target_location = {
			add_location_modifier = {
				modifier = mines_of_tzantzakon_modifier
				years = modifier_duration_years_long
				mode = replace
			}

			change_development = development_extreme_bonus

			ordered_pop = {
				limit = { pop_type = pop_type:peasants }
				order_by = pop_size
				max = 1
				check_range_bounds = no

				split_pop = {
					fraction = { 0.1 0.2 }
					type = pop_type:laborers
				}
			}
		}
	}

	option = {
		name = flavor_tre.7.b

		scope:target_location = {
			change_prosperity = prosperity_extreme_bonus
		}

		add_stability = stability_mild_bonus
	}
}
