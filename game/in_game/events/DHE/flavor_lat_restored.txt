namespace = flavor_lat

flavor_lat.1 = { # The New Latin Bureaucracy
    type = country_event
    title = flavor_lat.1.title
    desc = flavor_lat.1.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1360.1.1 
        to = 1600.1.1 
        monthly_chance = 5
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople       
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        root ?= {
            save_scope_as = latin_country
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }  
    }

     option = { # For a new distinct Latin Bureaucracy
        name = flavor_lat.1.a
        ai_chance = {
            base = 5
        } 
        add_legitimacy = legitimacy_mild_bonus
        add_country_modifier = { modifier = lat_a_distinct_latin_bureaucracy years = 25 mode = add }        
    }    

    option = { # Model the new bureaucracy around the old Crusader states
        name = flavor_lat.1.b
        ai_chance = {
            base = 5
        } 
        add_religious_influence = religious_influence_mild_bonus
        add_country_modifier = { modifier = lat_crusader_inspired_bureaucracy years = 25 mode = add }        
    } 

    option = { # Model the new bureaucracy after the Byzantines
        name = flavor_lat.1.c
        ai_chance = {
            base = 5
        } 
        add_legitimacy = legitimacy_mild_bonus
        add_country_modifier = { modifier = lat_byzantine_inspired_bureaucracy years = 25 mode = add }        
    }     

    option = { # Implement a Catalan inspired bureaucracy
        name = flavor_lat.1.d 
        high_risk_option = yes   
        trigger = {
            OR = {
                has_or_had_tag = ATH
                culture = culture:catalan
            }
        }      
        add_prestige = prestige_mild_bonus
        add_country_modifier = { modifier = lat_catalan_inspired_bureaucracy years = 25 mode = add }      
    }

    option = { # Implement a Neapolitan inspired bureaucracy
        name = flavor_lat.1.e    
        high_risk_option = yes
        trigger = {
            OR = {
                has_or_had_tag = ACH
                culture = culture:neapolitan
                culture = culture:french
            }
        }      
        add_stability = stability_mild_bonus
        add_country_modifier = { modifier = lat_neapolitan_inspired_bureaucracy years = 25 mode = add }      
    } 
}

flavor_lat.2 = { # The Rising Turkish Threat in Anatolia
    type = country_event
    title = flavor_lat.2.title
    desc = flavor_lat.2.desc
    outcome = neutral
    image = "gfx/interface/illustrations/situation/rise_of_the_ottomans.dds"

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 40
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        any_known_country = {
            is_neighbor_of = ROOT
            AND = {
                culture = culture:turkish_culture
                num_locations > 20
                OR = {
                    has_ruler = yes
                    has_regent = yes
                }
            }
        }
    }

    immediate = {
        ruler ?= {
            save_scope_as = scope_ruler
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        } 
        random_known_country = {
            limit = {
                is_neighbor_of = ROOT
                AND = {
                    culture = culture:turkish_culture
                    num_locations > 20 
                    OR = {
                        has_ruler = yes
                        has_regent = yes
                    }
                }
            }
            save_scope_as = rising_turkish_country
            ruler_or_regent ?= {
                save_scope_as = rising_turkish_country_ruler
            }
        }  
    }
    

    option = { # We shall accomplish what our predecessors and drive back the Beyliks!
        name = flavor_lat.2.a         
        change_societal_value = { type = offensive_vs_defensive value = societal_value_move_to_left }
        add_country_modifier = { modifier = lat_strike_the_beyliks years = 15 mode = add }   
        add_casus_belli = {
            target = scope:rising_turkish_country
            type = casus_belli:cb_annex
        }   
    }

    option = { # We should remain wary, and take a more defensive approach
        name = flavor_lat.2.b         
        change_societal_value = { type = offensive_vs_defensive value = societal_value_move_to_right }
        add_country_modifier = { modifier = lat_defensive_approach_to_anatolia years = 15 mode = add }      
    } 
}

flavor_lat.3 = { # Old Enemies of Bulgaria and Serbia
    type = country_event
    title = flavor_lat.3.title
    desc = flavor_lat.3.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 12
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        country_exists = c:SER
        country_exists = c:BUL
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        } 
        c:SER ?= {
            save_scope_as = serbia_country
        }
        c:BUL ?= {
            save_scope_as = bul_country
        }
    }
    

    option = { # We shall expand the empire to its rightful borders!
        name = flavor_lat.3.a         
        drop_antagonism_bomb = {
            target = capital
            modifier = antagonism_pushed_towards_belligerent
            value = 1.5
        }  
        add_casus_belli = {
            target = scope:serbia_country
            type = casus_belli:cb_annex
        }
        add_casus_belli = {
            target = scope:bul_country
            type = casus_belli:cb_annex
        }
    }

    option = { # Let us avoid repeating the mistakes of the past.
        name = flavor_lat.3.b         
        add_stability = stability_extreme_bonus
        add_country_modifier = { modifier = lat_avoided_bulgaria_serbia_campaigns years = 20 mode = add }
    } 
}

flavor_lat.4 = { # Restoration of the Holy Patriarchate of Constantinople
    type = country_event
    title = flavor_lat.4.title
    desc = flavor_lat.4.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 8
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        religion = religion:catholic
        country_exists = c:PAP
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:clergy_estate background = estate_type:clergy_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:PAP ?= {
            save_scope_as = pap_country
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        } 
        character:byz_michael_viii_palaiologos = {
            save_scope_as = scope_michael_palaiologos
        }
    }
    

    option = { # Restore the Patriarchate to Constantinople 
        name = flavor_lat.4.a   
        add_legitimacy = legitimacy_mild_bonus
        unlock_estate_privilege_effect = { type = lat_protector_of_the_constantinople_patriarchate }        
        location:constantinople = {
            change_development = development_mild_bonus
            add_location_modifier = {
                modifier = lat_restoration_of_the_constantinople_patriarchate
                years = -1
                mode = add
            }
            create_holy_site = {
                name = latin_patriarchate_of_constantinople
                type = christian_holy_site
                importance = 1
                location = this
                religions = { religion:catholic }
            }
        }
        add_opinion = { target = c:PAP modifier = lat_patriarchate_of_constantinople_restored }
        c:PAP = {
            add_opinion = { target = c:LAT modifier = lat_patriarchate_of_constantinople_restored }
        } 
    }

    option = { # We shall ensure the protection of the restored Patriarchate
        name = flavor_lat.4.b 
        add_prestige = prestige_mild_bonus
        unlock_estate_privilege_effect = { type = lat_protector_of_the_constantinople_patriarchate }   
        location:constantinople = {
            change_development = development_mild_bonus
            add_location_modifier = {
                modifier = lat_restoration_of_the_constantinople_patriarchate
                years = -1
                mode = add
            }
            create_holy_site = {
                name = latin_patriarchate_of_constantinople
                type = christian_holy_site
                importance = 1
                location = this
                religions = { religion:catholic }
            }
        }
        add_opinion = { target = c:PAP modifier = lat_patriarchate_of_constantinople_restored }
        c:PAP = {
            add_opinion = { target = c:LAT modifier = lat_patriarchate_of_constantinople_restored }
        }  
        grant_estate_privilege = estate_privilege:lat_protector_of_the_constantinople_patriarchate       
    } 

    option = { # Leverage the restored office for political gain.
        name = flavor_lat.4.c
        add_legitimacy = legitimacy_mild_penalty 
        unlock_estate_privilege_effect = { type = lat_protector_of_the_constantinople_patriarchate }   
        location:constantinople = {
            change_development = development_mild_bonus
            add_location_modifier = {
                modifier = lat_restoration_of_the_constantinople_patriarchate
                years = -1
                mode = add
            }
            create_holy_site = {
                name = latin_patriarchate_of_constantinople
                type = christian_holy_site
                importance = 1
                location = this
                religions = { religion:catholic }
            }
        }
        add_opinion = { target = c:PAP modifier = lat_patriarchate_of_constantinople_restored }
        c:PAP = {
            add_opinion = { target = c:LAT modifier = lat_patriarchate_of_constantinople_restored }
        }
        add_country_modifier = { modifier = lat_leveraged_patriarchate_for_estate_gain years = 10 mode = add }         
    } 

    option = { # Refuse to restore the Patriarchate
        name = flavor_lat.4.d
        add_legitimacy = legitimacy_severe_penalty 
        add_country_modifier = { modifier = lat_refused_to_restore_patriarchate years = 25 mode = add_and_extend }          
    }          
}

flavor_lat.5 = { # Control of Anatolia Reasserted
    type = country_event
    title = flavor_lat.5.title
    desc = flavor_lat.5.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 18
    }


    trigger = {
        at_war = no
        region:anatolia_region = {
            any_location_in_region = {
                owner ?= {
                    top_overlord_or_this ?= ROOT
                }
                count > 40
            }
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
    }
    

    option = { # Glory to the Latin empire!
        name = flavor_lat.5.a  
        add_stability = stability_extreme_bonus
        custom_tooltip = {
            text = flavor_lat_anatolia_secured
            region:anatolia_region = {
                every_location_in_region = {
                    limit = {
                        OR = {
                            owner ?= root
                            owner ?= { is_subject_of = root }
                        }
                    }
                    add_location_modifier = { modifier = lat_improved_location_integration years = 15 mode = add }
                }
            }
        }    
    }
}

flavor_lat.6 = { # Bulgaria Crushed
    type = country_event
    title = flavor_lat.6.title
    desc = flavor_lat.6.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 18
    }


    trigger = {
        at_war = no
        region:balkan_region = {
            any_location_in_region = {
                owner ?= {
                    top_overlord_or_this ?= c:BUL
                }
                count < 20
            }
        }
        region:balkan_region = {
            any_location_in_region = {
                is_core_of = c:BUL
                owner ?= {
                    top_overlord_or_this ?= ROOT
                }
                count > 15
            }
        }
        country_exists = c:BUL
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:BUL ?= {
            save_scope_as = target_country
        }
    }
    

    option = { # We shall expand the empire to its rightful borders!
        name = flavor_lat.6.a  
        add_prestige = prestige_extreme_bonus 
        custom_tooltip = {
            text = flavor_lat_balkan_rival_lands_secured
            region:balkan_region = {
                every_location_in_region = {
                    limit = {
                        is_core_of = scope:target_country
                        OR = {
                            owner ?= root
                            owner ?= { is_subject_of = root }
                        }
                    }
                    add_location_modifier = { modifier = lat_improved_location_integration years = 15 mode = add }
                }
            }
        }           
    }
}

flavor_lat.7 = { # Serbia Ambitions Defeated
    type = country_event
    title = flavor_lat.7.title
    desc = flavor_lat.7.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 18
    }


    trigger = {
        at_war = no
        region:balkan_region = {
            any_location_in_region = {
                owner ?= {
                    top_overlord_or_this ?= c:SER
                }
                count < 18
            }
        }
        region:balkan_region = {
            any_location_in_region = {
                is_core_of = c:SER
                owner ?= {
                    top_overlord_or_this ?= ROOT
                }
                count > 12
            }
        }
        country_exists = c:SER
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_poptype_effect = { foreground = pop_type:soldiers background = pop_type:soldiers }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        c:SER ?= {
            save_scope_as = target_country
        }
    }
    

    option = { # We shall expand the empire to its rightful borders!
        name = flavor_lat.7.a  
        add_prestige = prestige_extreme_bonus 
        custom_tooltip = {
            text = flavor_lat_balkan_rival_lands_secured
            region:balkan_region = {
                every_location_in_region = {
                    limit = {
                        is_core_of = scope:target_country
                        OR = {
                            owner ?= root
                            owner ?= { is_subject_of = root }
                        }
                    }
                    add_location_modifier = { modifier = lat_improved_location_integration years = 15 mode = add }
                }
            }
        }           
    }
}

flavor_lat.8 = { # Old Byzantine Elite
    type = country_event
    title = flavor_lat.8.title
    desc = flavor_lat.8.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 5
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople    
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        } 
    }
    

    option = { # It is time to lay the old empire to rest.
        name = flavor_lat.8.a  
        add_prestige = prestige_severe_bonus
        culture:greek_culture = { add_cultural_tradition = cultural_tradition_radical_penalty }  
        culture = { add_cultural_influence = cultural_influence_ultimate_bonus }   
        add_country_modifier = { modifier = lat_end_of_byzantine_culture years = 25 mode = add }  
    }

    option = { # Preserve aspects of Byzantine culture in the new Latin empire.
        name = flavor_lat.8.b  
        add_prestige = prestige_mild_penalty
        culture:greek_culture = { add_cultural_tradition = cultural_tradition_ultimate_penalty }  
        culture = { add_cultural_influence = cultural_influence_extreme_bonus }   
        add_country_modifier = { modifier = lat_legacy_of_rome years = 25 mode = add }  
    }    
}

flavor_lat.9 = { # Papal Request Conversion of Byzantine Lands
    type = country_event
    title = flavor_lat.9.title
    desc = flavor_lat.9.desc
    outcome = neutral

    fire_only_once = yes
    dynamic_historical_event = {
        tag = LAT
        from = 1338.1.1 
        to = 1600.1.1 
        monthly_chance = 6
    }


    trigger = {
        at_war = no
        has_ruler = yes
        owns = location:constantinople
        religion = religion:catholic
        country_exists = c:PAP
        custom_tooltip = {
            text = lat_accepted_pope_latin_empire_proposal_tt
            has_variable = lat_accepted_pope_latin_empire_proposal
        }
        c:PAP ?= {
            has_ruler = yes
        }
    }

    illustration_tags = {
        10 = regular
        10 = interior
    }	

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:clergy_estate background = estate_type:clergy_estate }
        ruler ?= {
            save_scope_as = scope_ruler
        }
        unlock_law_effect = {
            type = lat_orthodox_status_with_the_empire
        }
        location:constantinople = {
            save_scope_as = constantinople_location
        }
        c:PAP ?= {
            save_scope_as = pap_country
            ruler ?= {
                save_scope_as = scope_pope_ruler
            }
        }
    }
    

    option = { # Honor our promise to the Pope
        name = flavor_lat.9.a  
        add_legitimacy = legitimacy_mild_bonus
        add_religious_influence = religious_influence_extreme_bonus
        show_as_tooltip = {
            unlock_law_effect = {
                type = lat_orthodox_status_with_the_empire
            }
        }
        add_policy = policy:lat_papal_sanctioned_conversion_efforts
    }

    option = { # Allow conversion efforts, but do not fully sanction them
        name = flavor_lat.9.b
        add_legitimacy = legitimacy_mild_penalty  
        add_religious_influence = religious_influence_mild_penalty
        show_as_tooltip = {
            unlock_law_effect = {
                type = lat_orthodox_status_with_the_empire
            }
        }
        add_policy = policy:lat_limited_conversion_efforts
    }    

    option = { # Allow conversion efforts, but do not fully sanction them
        name = flavor_lat.9.c
        add_legitimacy = legitimacy_extreme_penalty  
        add_religious_influence = religious_influence_extreme_penalty
        show_as_tooltip = {
            unlock_law_effect = {
                type = lat_orthodox_status_with_the_empire
            }
        }
        add_policy = policy:lat_tolerance_orthodox_faith
    }      
}
