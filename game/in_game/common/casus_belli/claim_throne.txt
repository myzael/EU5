#hardcoded, do not remove it!
cb_claim_throne = {
	
	create_visible = {
		always = no
	}
	create_enabled = {
		always = no
	}
	declare_enabled = {
		#The claim throne cb is not given to if these triggers are not fulfilled
		government_type = government_type:monarchy
		scope:target = {
			government_type = government_type:monarchy
		}
		scope:target_character ?= { #has to exist
			is_alive = yes #not dead
			not = { this = scope:target.ruler } #not already the ruler
		}
	}

	war_goal_type = take_capital_claim_throne
	additional_war_enthusiasm_attacker = {
		if = {
			limit = {
				exists = scope:target
				scope:target = { is_alive = no }
			}
			value = -100	#why do we even keep fighting?
		}
		else = {
			value = 0
		}
	}

	ai_will_do = {
		add = {
			desc = "CB_REASON_IMPORTANT_CB"
			value = 100	#AI should highly prefer this cb over others
		}
	}
}
