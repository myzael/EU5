cb_fabricated_conquer_province = {
	years = 15
	create_visible = { always = no }
	create_enabled = {
		scope:target = {
			any_owned_location = {
				is_core_of = root
			}
		}
	}
	
	#scopes are actor/recipient for countries, while root = province checked.
	province = {
		any_location_in_province = {
			is_core_of = scope:actor
		}
	}

	war_goal_type = fabricated_conquer_province
}
