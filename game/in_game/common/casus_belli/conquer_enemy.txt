#root is country
#scope:target is the target 

cb_conquer_enemy = {
	create_visible = {
		scope:target = {
			is_enemy_of = root
		}
	}
	create_enabled = {
		not = {
			has_truce_with = scope:target
		}
	}
	declare_enabled = {
		scope:target = {
			is_enemy_of = root
		}
	}

	war_goal_type = superiority_rival
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 10
		}
	}
}
