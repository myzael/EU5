#root is country
#scope:target is the target 


cb_independence_war = {
	visible = {
		is_subject = yes
	}
	allow_creation = {
		is_subject = yes
		overlord = scope:target
	}
	allow_declaration = {
		is_subject = yes
		overlord = scope:target
	}
	
	war_goal_type = independence
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 10
		}
	}
}
