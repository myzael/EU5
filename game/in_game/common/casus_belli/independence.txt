#root is country
#scope:target is the target 


cb_independence_war = {
	create_visible = {
		is_subject = yes
	}
	create_enabled = {
		is_subject = yes
		overlord = scope:target
	}
	declare_enabled = {
		is_subject = yes
		overlord = scope:target
	}
	
	custom_tags = { cannot_be_enforced_peace_on }
	war_goal_type = independence
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 10
		}
	}
}
