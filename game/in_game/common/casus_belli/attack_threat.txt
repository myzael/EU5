#root is country
#scope:target is the target 

attack_threat = {
	create_visible = {
		scope:target = {
			is_rival_of = root
			
		}
		is_a_threat_for_us = scope:target
	}
	create_enabled = {
		not = {
			has_truce_with = scope:target
		}
	}
	declare_enabled = {
		scope:target = {
			is_rival_of = root
			
		}
		is_a_threat_for_us = scope:target
	}

	war_goal_type = superiority_rival
	
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 10
		}
	}
}
