#root is country
#scope:target is the target 


cb_disloyal_subject = {
	create_visible = {
		is_subject = no
		scope:target = {
			is_subject = yes
			overlord = root
		}
	}

	create_enabled = {
		scope:target = {
			subject_loyalty < 50
		}
	}

	declare_enabled = {
		is_subject = no
		scope:target = {
			is_subject = yes
			overlord = root
		}
	}

	allow_wars_on_own_subjects = yes
	war_goal_type = take_capital
}
