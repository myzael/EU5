#root is country
#scope:target is the target 


cb_slave_raiding = {
	create_visible = { modifier:auto_slave_raid = yes }
	create_enabled = {
		not = {
			has_truce_with = scope:target
		}
	}

	speed = 8.0

	war_goal_type = humiliate

	#disable ai from using this for now as it leads to lots of useless wars
	ai_will_do = {
		value = -1
	}
}
