#root is country
#scope:target is the target 

cb_humiliate_rival = {
	create_visible = {
		scope:target = {
			is_rival_of = root
		}
	}
	create_enabled = {
		not = {
			has_truce_with = scope:target
		}
	}
	declare_enabled = {
		scope:target = {
			is_rival_of = root
		}
	}

	war_goal_type = humiliate
	ai_will_do = {
		add = {
			desc = "BASE"
			value = -10
		}
	} # can cause a lot of useless wars for ai
}
