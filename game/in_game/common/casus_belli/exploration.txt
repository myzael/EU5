#root is country
#scope:target is the target 


cb_exploration = {

	create_visible = {
		OR = {
			has_advance = exploration
			has_advance = chartered_companies
		}
	}
	
	create_enabled = {
		NOT = { capital = { sub_continent = scope:target.capital.sub_continent } }
		power_projection > scope:target.power_projection
	}
	
	war_goal_type = superiority
}
