unit_formation_preference = {
	name = {
		widget = unit_formation_preference_name
		width = 150
		is_constant_width = no
		contains_select_target_button = yes
		single_widget_for_row = yes
		sort = {
			sort_key = "name"
			sort_text = {
				if = {
					limit = {
						exists = root
					}
					value = "SORT_TEXT_UNIT_FORMATION_PREFERENCE_NAME"
				}
				else = {
					value = "MODIFIER_NONE"
				}
			}
			sort_by_tooltip_key = "UNIT_FORMATION_PREFERENCE_SORT_BY_NAME"
		}
		sort = {
			sort_key = "effect"
			sort_text = {
				if = {
					limit = {
						exists = root
					}
					value = "SORT_TEXT_UNIT_FORMATION_PREFERENCE_EFFECT"
				}
				else = {
					value = "MODIFIER_NONE"
				}
			}
			sort_by_tooltip_key = "UNIT_FORMATION_PREFERENCE_SORT_BY_EFFECT"
		}
	}
}