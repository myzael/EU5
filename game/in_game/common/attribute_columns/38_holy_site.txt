holy_site = {
	name = {
		widget = holy_site_name
		sort = {
			sort_text = {
				if = {
					limit = {
						exists = root
					}
					value = "SORT_TEXT_HOLY_SITE_NAME"
				}
				else = {
					value = "MODIFIER_NONE"
				}
			}
			sort_by_tooltip_key = "SORT_TEXT_NAME_TT"
		}
	}

	owner_flag = {
		widget = holy_site_owner_flag
		width = 40
		is_constant_width = yes
		sort = {
			sort_text = {
				if = {
					limit = {
						exists = root
					}
					value = "SORT_TEXT_HOLY_SITE_OWNER_NAME"
				}
				else = {
					value = "MODIFIER_NONE"
				}
			}
			sort_by_tooltip_key = "LOCATION_SORT_BY_OWNER"
		}
	}

	time = {
		widget = holy_site_time
		width = 100
		is_constant_width = yes
		sort = {
			sort_value = {
				value = "scope:actor.capital.distance_to_squared(root.location)"
				multiply = 0.00005
				add = 2
			}
			sort_by_tooltip_key = "HOLY_SITE_SORT_BY_TRAVEL_MONTHS"
		}
	}

	gold = {
		widget = holy_site_gold
		width = 60
		is_constant_width = yes
		sort = {
			sort_value = {
				value = "scope:actor.capital.distance_to_squared(root.location)"
				multiply = 0.0001
				add = 1
			}
			sort_by_tooltip_key = "HOLY_SITE_SORT_BY_GOLD_COST"
		}
	}

	influence = {
		widget = holy_site_influence
		width = 60
		is_constant_width = yes
		sort = {
			sort_value = {
				value = "scope:actor.capital.distance_to_squared(root.location)"
				multiply = 0.0001
				add = 10
			}
			sort_by_tooltip_key = "HOLY_SITE_SORT_BY_INFLUENCE_GAIN"
		}
	}
}
