war = {
	name = {
		widget = war_name
		width = 150
		fixed_height = 32
		is_constant_width = no
		sort = {
			sort_text = {
				if = {
					limit = {
						exists = root
					}
					value = "SORT_TEXT_WAR_NAME"
				}
				else = {
					value = "MODIFIER_NONE"
				}
			}
			sort_by_tooltip_key = "SORT_TEXT_NAME_TT"
		}
	}

	select_target_acceptance_total = {
		widget = select_target_acceptance_total
		width = 80
		fixed_height = 32
		sort = {
			sort_by_acceptance = yes
			sort_by_tooltip_key = SELECT_TARGET_SORT_BY_ACCEPTANCE_TOTAL
			sort_order = "Descending"
		}
	}
}