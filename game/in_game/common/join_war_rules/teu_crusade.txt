TEU_POL_BOH_truce = {
    join_war_disabled_trigger = {
        OR = {
            tag = POL
            tag = BOH
        }
        exists = scope:war
        exists = scope:war.var:teu_lit_first_war
    }
}