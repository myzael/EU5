italian_league_sponsor = {
	priority = 1

	max_countries = 2

	auto_bestowal_trigger = { always = no }
	auto_rescind_trigger = {
		OR = {
			is_subject = yes
			any_country_at_war_with = {
				AND = {
					root = { is_member_of_international_organization = scope:recipient }
					is_member_of_international_organization = scope:recipient
				}
			}
			scope:recipient.leader_country ?= {
				OR = {
					is_rival_of = root
					is_enemy_of = root
				}
			}
		}
	}
}
