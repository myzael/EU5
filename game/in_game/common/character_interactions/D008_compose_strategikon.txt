compose_strategikon = {
	message = yes
	on_own_nation = yes

	potential = {
		scope:actor = {
			has_or_had_tag = BYZ
			has_dlc = "d008_fate_of_the_phoenix"
		}
	} 

	ai_tick = monthly
	ai_tick_frequency = 6

	price = price:compose_strategikon_price

	allow = {
		scope:actor = {
			has_ruler = yes

			ruler ?= { 
				age_in_years > 35 		#We don't want babies writing thesis on war
				mil >= 75
			}
		}

		custom_tooltip = {
			text = has_not_composed_strategikon_tt
			scope:actor.ruler ?= {
				NOT = { 
					has_variable = compose_strategikon_variable 
				}
			}
		}
	}

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "choose_character"
		column = {
			data = name
		}
		visible = {
			is_ruler = yes
		}
	}

	effect = {
		scope:recipient ?= {
			hidden_effect = { set_variable = compose_strategikon_variable }
			random_list = {
				1 = {
					add_character_modifier = {
						modifier = strategikon_synoro_modifier
						years = -1
						mode = replace
						size = {
							add = {
								value = 1
								desc = BASE_VALUE
							}
							add = {
								value = this.total_abilities
								divide = 300
								desc = actor_ruler_total_abilities
							}
						}
					}
				}
				1 = {
					add_character_modifier = {
						modifier = strategikon_pezikou_modifier
						years = -1
						mode = replace
						size = {
							add = {
								value = 1
								desc = BASE_VALUE
							}
							add = {
								value = this.total_abilities
								divide = 300
								desc = actor_ruler_total_abilities
							}
						}
					}
				}
				1 = {
					add_character_modifier = {
						modifier = strategikon_thalassas_modifier
						years = -1
						mode = replace
						size = {
							add = {
								value = 1
								desc = BASE_VALUE
							}
							add = {
								value = this.total_abilities
								divide = 300
								desc = actor_ruler_total_abilities
							}
						}
					}
				}
				1 = {
					add_character_modifier = {
						modifier = strategikon_ippikou_modifier
						years = -1
						mode = replace
						size = {
							add = {
								value = 1
								desc = BASE_VALUE
							}
							add = {
								value = this.total_abilities
								divide = 300
								desc = actor_ruler_total_abilities
							}
						}
					}
				}	
			}
		}
	}

   ai_will_do = {
		add = {
			value = 100
			desc = BASE_VALUE
		}
    }
}