appease_estate = {
	ability = dip
	allow_multiple = no
	
	potential = {
	}
	
	allow = {
	}

	select_trigger = {
		looking_for_a = estate
		source = actor
		target_flag = target
		name = "mission_select_estate"
		column = {
			data = name
		}
        visible = {
            estate_type = {
                always_loyal = no
            }
            scope:actor = {
                country_has_estate = root.estate_type
            }
        }
		enabled = {
			satisfaction < 0.5
		}
	}

    # Pre-selection: display only, never applies. always = no prevents application.
    country_modifier = {
        display_trigger = {
            NOT = { exists = scope:target }
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_selected_estate_will_receive
                always = no
            }
        }
        global_estate_satisfaction_recovery = 0.001
        global_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    # Post-selection: show and apply the modifier for the selected estate only.
    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:nobles_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:nobles_estate
            }
        }
        nobles_estate_satisfaction_recovery = 0.001
        nobles_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:clergy_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:clergy_estate
            }
        }
        clergy_estate_satisfaction_recovery = 0.001
        clergy_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:burghers_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:burghers_estate
            }
        }
        burghers_estate_satisfaction_recovery = 0.001
        burghers_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:peasants_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:peasants_estate
            }
        }
        peasants_estate_satisfaction_recovery = 0.001
        peasants_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:dhimmi_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:dhimmi_estate
            }
        }
        dhimmi_estate_satisfaction_recovery = 0.001
        dhimmi_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:tribes_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:tribes_estate
            }
        }
        tribes_estate_satisfaction_recovery = 0.001
        tribes_estate_target_satisfaction = small_permanent_target_satisfaction
    }

    country_modifier = {
        display_trigger = {
            exists = scope:target
            scope:target.estate_type = estate_type:cossacks_estate
        }
        potential_trigger = {
            custom_tooltip = {
                text = appease_estate_active_modifier
                exists = scope:target
                scope:target.estate_type = estate_type:cossacks_estate
            }
        }
        cossacks_estate_satisfaction_recovery = 0.001
        cossacks_estate_target_satisfaction = small_permanent_target_satisfaction
    }

	is_finished = {
		scope:target = {
			satisfaction >= 0.5
		}
	}

	progress = {
		scope:target = {
			value = satisfaction
		}
	}
}
