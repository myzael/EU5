train_admiral = {
	type = owncountry

	price = price:train_admiral

	ai_tick = daily
	ai_tick_frequency = 180

	automation_tick = daily
	automation_tick_frequency = 20
	player_automated_category = replaceadmirals


	potential = {
		scope:actor = {
			has_ports = yes
		}
	}

	allow = {
	}

	ai_prerequisite = {
		navy_size > 0
	}

	ai_will_do = {
		scope:actor = {
			#first, how many do we need?
			value = {
				#how many should we have?
				value = {
					value = navy_size
					divide = {
						value = 4
						multiply = "define:NAI|AVERAGE_FRONTAGE"
					}
				}

				#if we're at war, anticipate more need
				if = {
					limit = {
						at_war = yes
					}
					add = 1
				}
				subtract = possible_military_leaders
			}
		}
	}

	effect = {
		scope:actor = {
			custom_tooltip = {
				text = train_admiral_effect
				create_character = {
					important_character_counts = none
					skip_update_inheritance = yes
					skip_on_created_action = yes
					use_random_core_location = yes
					trait_category = admiral
					adm = {
						integer_range = {
							min = {
								#10 for no navy tradition, 60 for full
								value = 10
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
							max = {
								#50 for no navy tradition, 100 for full
								value = 50
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
						}

						if = {
							limit = {
								scope:actor = { has_country_modifier = reformation_of_the_school_of_admirals_modifier }
							}
							add = 10
						}
						if = {
							limit = { scope:actor.modifier:train_admiral_ability != 0 }
							add = scope:actor.modifier:train_admiral_ability
						}
					}
					dip = {
						integer_range = {
							min = {
								#10 for no navy tradition, 60 for full
								value = 10
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
							max = {
								#50 for no navy tradition, 100 for full
								value = 50
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
						}

						if = {
							limit = {
								scope:actor = { has_country_modifier = reformation_of_the_school_of_admirals_modifier }
							}
							add = 10
						}
						if = {
							limit = { scope:actor.modifier:train_admiral_ability != 0 }
							add = scope:actor.modifier:train_admiral_ability
						}
					}
					mil = {
						integer_range = {
							min = {
								#10 for no navy tradition, 60 for full
								value = 10
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
							max = {
								#50 for no navy tradition, 100 for full
								value = 50
								add = {
									value = navy_tradition
									multiply = 0.5
								}
							}
						}

						if = {
							limit = {
								scope:actor = { has_country_modifier = reformation_of_the_school_of_admirals_modifier }
							}
							add = 10
						}
						if = {
							limit = { scope:actor.modifier:train_admiral_ability != 0 }
							add = scope:actor.modifier:train_admiral_ability
						}
					}
					#they can't be in the cabinet
					add_character_modifier = {
						modifier = recruited_in_the_military
						mode = add_and_extend
					}

					random_estate_type_trigger = {
						scope:actor = { estate_type_allowed_in_command = { estate_type = prev } }
					}

					if = {
						limit = {
							scope:actor = { has_country_modifier = reformation_of_the_school_of_admirals_modifier }
						}

						custom_tooltip = better_stats_because_reformation_of_the_school_of_admirals_modifier_tt
					}

					save_scope_as = new_character
				}
			}
		}
	}
}
