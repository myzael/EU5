revoke_town_rights = {
	type = owncountry
	
	show_message = no
	
	price = price:revoke_town_rights

	ai_tick = never
	automation_tick = never

	effect = {
		scope:target = {
			location = {
				every_pop = {
					limit = {
						pop_type = pop_type:burghers
					}
					add_pop_satisfaction = pop_satisfaction_ultimate_penalty
				}
			}
		}
		revoke_town_rights = scope:target
	}

}
