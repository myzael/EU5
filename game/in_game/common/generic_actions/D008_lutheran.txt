contact_patriarch_of_constantinople = {
	type = situation
	sound = UI_action_religion_generic

	potential = {
		has_dlc = "d008_fate_of_the_phoenix"
		NOT = { has_global_variable = reformation_patriarch_contacted }
		location:constantinople = {
			has_owner = yes
			owner = { religion = religion:orthodox }
		}
	}

	allow = {
		is_situation_active = situation:reformation
		scope:actor = { religion = religion:lutheran }
		character:reformer_philip_melanchton ?= {
			is_alive = yes
			owner = scope:actor
		}
	}

	select_trigger = {
		looking_for_a = situation
		target_flag = recipient
		name = "choose_situation"
		column = {
			data = name
		}
		visible = {
			situation:reformation = this
			situation_is_active = yes
		}
	}

	price = price:contact_patriarch_of_constantinople

	ai_tick = monthly
	ai_tick_frequency = 12

	effect = {
		scope:actor = {
			set_global_variable = reformation_patriarch_contacted
			trigger_event_non_silently = reformation.17
		}
	}

	ai_will_do = {
		value = 10
		if = {
			limit = {
				scope:actor = {
					any_country = {
						religion = religion:orthodox
						is_rival_of = scope:actor
					}
				}
			}
			multiply = 0
		}
	}
}
