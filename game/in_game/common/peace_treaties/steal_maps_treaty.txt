steal_maps_treaty = {
	collate_targets = yes #this doesn't depend on the giver, so we can collate all the target areas uniquely

	potential = {
	}

    allow = {
	}

	select_trigger = {
		looking_for_a = area
		target_flag = target
		name = "steal_maps_select_title"
		none_available_msg_key = "steal_maps_no_areas"
		column = {
			data = name
		}
		visible = {
			scope:recipient = { has_discovered_area = prev }
			NOT = { scope:actor = { has_discovered_area = prev } }
		}
		enabled = {
			any_neighbor_area = {
				scope:actor = { has_discovered_area = prev }
			}
		}
	}

    cost = {
		add = {
			desc = "DIPLOREASON_BASE"
			value = 10
		}
	}
	
	effect = {
		if = {
			limit = {
                exists = scope:target
            }
			scope:winner = {
				discover_area = scope:target
			}
		}
	}

	ai_desire = {
		
	}
}

