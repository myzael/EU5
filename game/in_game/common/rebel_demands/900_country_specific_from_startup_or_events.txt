the_goals_of_balliol = {
	trigger = {
		always = no 	# already set from start of the game..
	}
	
	concession_effect = {
		# civil war already no need.
	}
	victory_effect = {
		add_legitimacy = legitimacy_mild_bonus
		c:ENG = { add_opinion = { target = root modifier = merchant_appeasement  } }
	}
}