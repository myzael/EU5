########################
# Tradition
########################

a_footmen = {
	default = yes
	copy_from = a_age_1_traditions_heavy_infantry
	strength_damage_taken = -0.05
	
	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}
	
	upgrades_to = a_men_at_arms

	
	maintenance_demand =  early_heavy_infantry_maintenance
	construction_demand = early_heavy_infantry_construction
	
	gfx_tags = { medium_tag }
	

}

a_archers = {
	default = yes
	copy_from = a_age_1_traditions_light_infantry
	

	upgrades_to = a_crossbowmen

	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	maintenance_demand = archer_infantry_maintenance
	construction_demand = archer_infantry_construction
	gfx_tags = { archer_tag medium_tag }
}

a_footmen_levy = {
	copy_from = a_footmen
	
	buildable = no
	levy = yes
	gfx_tags = { medium_tag }
}

a_horsemen = {
	copy_from = a_age_1_traditions_light_cavalry
	
	
	upgrades_to = a_cavalrymen

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.01
	}
	gfx_tags = { medium_tag }
}

a_armored_horsemen = {
	copy_from = a_age_1_traditions_heavy_cavalry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.1
	}
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction

	upgrades_to = a_heavy_cavalrymen

	gfx_tags = { heavy_tag }
}

a_camp_followers = {
	copy_from = a_age_1_traditions_auxiliary
	gfx_tags = { medium_tag }
	
	country_potential = { 
		and = {
			exists = government_type
			government_type != government_type:steppe_horde
		}
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	
	upgrades_to = a_supply_carts

}

########################
# Renaissance
########################

a_men_at_arms = {
	copy_from = a_age_2_renaissance_heavy_infantry
	strength_damage_taken = -0.1
	combat_speed = 1

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}

	
	upgrades_to = a_halberdiers

	
	maintenance_demand =  early_heavy_infantry_maintenance
	construction_demand = early_heavy_infantry_construction
	
	gfx_tags = { medium_tag }
}

a_crossbowmen = {
	copy_from = a_age_2_renaissance_light_infantry
	
	
	upgrades_to = a_early_arquebusiers


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}

	gfx_tags = { crossbow_tag medium_tag }
}

a_handgonners = {
	copy_from = a_age_2_renaissance_heavy_infantry
	
	morale_damage_done = 0.10
	build_time_modifier = 0.25
	combat_speed = -0.5

	
	maintenance_demand =  infantry_maintenance
	construction_demand = infantry_construction
	
	upgrades_to = a_early_arquebusiers


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.025
	}
	gfx_tags = { musket_tag medium_tag }
}

a_men_at_arms_levy = {
	copy_from = a_age_2_renaissance_heavy_infantry
	buildable = no
	levy = yes
	
	strength_damage_taken = -0.1
	combat_speed = 1

	gfx_tags = { medium_tag }
}

a_cavalrymen = {
	copy_from = a_age_2_renaissance_light_cavalry

	
	
	upgrades_to = a_light_lancers


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.01
	}

	
	gfx_tags = { medium_tag }
}

a_heavy_cavalrymen = {
	copy_from = a_age_2_renaissance_heavy_cavalry


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	
	upgrades_to = a_lancers
	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction
	gfx_tags = { heavy_tag }
}

a_houfnice = {
	copy_from = a_age_2_renaissance_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	
	upgrades_to = a_falconet
}

a_peasant_levy = {
	copy_from = a_age_2_renaissance_light_infantry
	buildable = no
	levy = yes
	gfx_tags = { light_tag }
}

a_supply_carts = {
	copy_from = a_age_2_renaissance_auxiliary

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	upgrades_to = a_supply_convoy


}


########################
# Discovery
########################

a_early_arquebusiers = {
	copy_from = a_age_3_discovery_light_infantry
	

	upgrades_to = a_arquebusiers
	
	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}

	gfx_tags = { musket_tag medium_tag }
}

a_halberdiers = {
	copy_from = a_age_3_discovery_heavy_infantry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}

	upgrades_to = a_pikemen


	maintenance_demand =  early_heavy_infantry_maintenance
	construction_demand = early_heavy_infantry_construction

	gfx_tags = { polearm_tag medium_tag }
}

a_light_lancers = {
	copy_from = a_age_3_discovery_light_cavalry
	
	upgrades_to = a_pistoleers


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.01
	}

	
	gfx_tags = { spear_tag medium_tag }
}

a_lancers = {
	copy_from = a_age_3_discovery_heavy_cavalry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	
	upgrades_to = a_lancers
	upgrades_to = a_heavy_lancers

	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction
	
	gfx_tags = { spear_tag heavy_tag }
}

a_falconet = {
	copy_from = a_age_3_discovery_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}


	upgrades_to = a_chambered_cannon

}

a_matchlock_levy = {
	copy_from = a_age_3_discovery_light_infantry

	buildable = no
	levy = yes

	gfx_tags = { musket_tag light_tag }
}

a_supply_convoy = {
	copy_from = a_age_3_discovery_auxiliary

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}

	upgrades_to = a_baggage_train


}


########################
# Reformation
########################

a_arquebusiers = {
	copy_from = a_age_4_reformation_light_infantry

	upgrades_to = a_hunters
	

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}

	gfx_tags = { musket_tag medium_tag }
}

a_pikemen = {
	copy_from = a_age_4_reformation_heavy_infantry


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}

	
	upgrades_to = a_musketeers

	
	maintenance_demand =  pikemen_maintenance
	construction_demand = pikemen_construction

	gfx_tags = { pike_tag medium_tag }
}

a_pistoleers = {
	copy_from = a_age_4_reformation_light_cavalry
	
	upgrades_to = a_hussars


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	gfx_tags = { pistol_tag medium_tag }
}

a_heavy_lancers = {
	copy_from = a_age_4_reformation_heavy_cavalry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	gfx_tags = { spear_tag heavy_tag }
	
	upgrades_to = a_gallop_cavalry

	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction
}

a_chambered_cannon = {
	copy_from = a_age_4_reformation_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}

	upgrades_to = a_royal_mortar
}

a_flintlock_levy = {
	copy_from = a_age_4_reformation_light_infantry
	buildable = no
	levy = yes
	gfx_tags = { musket_tag light_tag }
}

a_baggage_train = {
	copy_from = a_age_4_reformation_auxiliary

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }
	
	upgrades_to = a_wagon_train

}


########################
# Absolutism
########################
a_hunters = {
	copy_from = a_age_5_absolutism_light_infantry

	upgrades_to = a_sharpshooters


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}

	gfx_tags = { rifle_tag medium_tag }
	color = unit_rifle_green
}

a_musketeers = {
	copy_from = a_age_5_absolutism_heavy_infantry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}

	
	upgrades_to = a_fusiliers
	
	maintenance_demand =  heavy_infantry_maintenance
	construction_demand = heavy_infantry_construction

	
	gfx_tags = { musket_tag medium_tag }
}

a_hussars = {
	copy_from = a_age_5_absolutism_light_cavalry
	
	
	upgrades_to = a_light_dragoons


	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	gfx_tags = { medium_tag }
}

a_gallop_cavalry = {
	copy_from = a_age_5_absolutism_heavy_cavalry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	
	upgrades_to = a_cuirassiers
	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction
	gfx_tags = { heavy_tag }
}

a_royal_mortar = {
	copy_from = a_age_5_absolutism_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}

	
	
	upgrades_to = a_flying_battery
}

a_militiamen = {
	copy_from = a_age_5_absolutism_light_infantry
	buildable = no
	levy = yes
	gfx_tags = { musket_tag light_tag }

}

a_wagon_train = {
	copy_from = a_age_5_absolutism_auxiliary

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	upgrades_to = a_logistics_corps
}


########################
# Revolutions
########################

a_sharpshooters = {
	copy_from = a_age_6_revolutions_light_infantry
	
	

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}

	gfx_tags = { rifle_tag medium_tag }
	color = unit_rifle_green 
}

a_fusiliers = {
	copy_from = a_age_6_revolutions_heavy_infantry

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}

	
	maintenance_demand =  heavy_infantry_maintenance
	construction_demand = heavy_infantry_construction

	gfx_tags = { musket_tag medium_tag }
}

a_light_dragoons = {
	copy_from = a_age_6_revolutions_light_cavalry
	
	gfx_tags = { heavy_tag }
}

a_cuirassiers = {
	copy_from = a_age_6_revolutions_heavy_cavalry
	gfx_tags = { cuirassier_tag heavy_tag }
	
	maintenance_demand =  heavy_cavalry_maintenance
	construction_demand = heavy_cavalry_construction
	
}

a_flying_battery = {
	copy_from = a_age_6_revolutions_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}

}

a_conscripts = {
	copy_from = a_age_6_revolutions_light_infantry
	buildable = no
	levy = yes
	gfx_tags = { musket_tag light_tag }
}

a_logistics_corps = {
	copy_from = a_age_6_revolutions_auxiliary

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }
}