mfa_join_against_byz = { 
	type = diplomacy
	category = CATEGORY_FRIENDLY_ACTIONS

	potential = {
		scope:actor = {
			has_variable = mfa_gathering_for_fight_with_byz
		}
		country_exists = c:BYZ
	}
	
	allow = {
	}
	
	ai_tick = never
	
	select_trigger = {
		looking_for_a = country
		target_flag = recipient
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
			is_subject = no
			has_ports = yes
			culture = {
				has_culture_group = culture_group:italian_group
			}
			NOT = {
				scope:actor = {
					is_target_in_variable_list = {
						name = mfa_war_participants
						target = ROOT
					}
				}
			}
		}
	}

	diplo_chance = {
		base = -40
		rank = -5
		diplomatic_reputation = 1
		opinion = 0.25
		border_distance = -0.05
		yesman = 100000
		relative_strength = -1
		negative_stability = -1
		competing_power = -10
	}

	effect = {
		if = {
			limit = {
				exists = scope:recipient
			}
			custom_tooltip = recipient_agrees_to_join_war
			scope:actor = {
				add_to_variable_list = {
					name = mfa_war_participants
					target = scope:recipient
				}
			}
		}
	}

	ai_will_do = {
	}
}	
